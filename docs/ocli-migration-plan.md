# Oases ocli Migration Plan

Last updated: 2026-06-18 03:03 Asia/Shanghai

## Goal

Turn the current `ocli-test` prototype tree into the official Oases desktop `ocli` runtime for project mode.

The end state is:

- Users install once with `npm install -g oases-ocli`, then run `ocli`.
- Oases Web stays deployed on Vercel and owns model selection, API proxying, chat UI, approvals UI, and session visualization.
- Local `ocli` owns workspace access, tools, command execution, Python execution, browser/network fetches, agent sessions, permissions, audit logs, and recoverable task state.
- `ocli` does not require local model provider setup or local API keys.
- Mature capabilities from `ocli/ocli-test/src` are progressively adapted into Oases protocol instead of keeping a small sidecar bridge forever.

## Current Architecture Snapshot

| Area | Current state | Target state |
| --- | --- | --- |
| Runtime entry | `ocli/index.js` starts official `ocli/src` runtime | `ocli/index.js` starts official `ocli` package/runtime |
| Active runtime code | `ocli/src/*.js` | `ocli/src/*.js` official package source |
| Mature prototype source | `ocli/ocli-test/src/**` | Migration source for tools, permissions, sessions, skills, agents, and orchestration |
| Old MVP source | `ocli/legacy-mvp-src/**` | Archived until no longer needed |
| npm package skeleton | `ocli/package.json` | `ocli/package.json` with `oases-ocli` bin |
| Web integration | Works via local HTTP/SSE bridge | Same protocol, richer agent behavior and better continuity |

## Phase Table

| Phase | Status | Objective | Main deliverables | Validation | Deployment |
| --- | --- | --- | --- | --- | --- |
| 0. Bridge MVP stabilization | Done | Prove Web can connect to local runtime and execute tools | `/health`, `/tools`, `/agent/sessions`, SSE, approvals, persistence, native tool calls | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` | Production deployed to `https://www.oasesai.xyz` |
| 1. Official ocli package structure | Done | Move from sidecar `ocli-test/oases-bridge` to official `ocli` runtime/package shape | `ocli/package.json`, `ocli/bin/ocli.js`, official runtime source, updated root entry, updated docs/tests | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed | Production deployed |
| 2. Tool system migration | Done | Adapt mature `ocli-test/src/tools` concepts into Oases tool registry | Tool adapters for artifacts, todo planning, improved fetch metadata, generated-file detection | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed | Production deployed |
| 3. Permission system migration | Done | Replace MVP risk checks with structured permission model | Permission categories, shell safety classification, per-session approval memory, clearer approval events, stronger destructive command detection | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Production deployed |
| 4. Continuous task orchestration | Done | Make project tasks continue until completion without user nudging | Task state, auto-continue slices, completion criteria, stronger max-turn handling | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Production deployed |
| 5. Session recovery and audit UX | Done | Make local sessions first-class recoverable engineering artifacts | Session detail panel, categorized timeline, artifact list, resume/continue from session | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Production deployed |
| 6. Skills, agents, and plugins | In progress | Bring in higher-level `ocli-test` orchestration | Skill loading, agent delegation, task/todo tools, plugin path strategy | Skill/tool integration smoke | Deploy after pass |
| 7. npm/GitHub release readiness | Done | Publishable CLI and GitHub workflow | License/repository fields, release workflow, npm dry-run, install docs, npm registry release | `oases-ocli@0.1.0` published to npm; real `npm install -g oases-ocli` smoke passed | Published |
| 8. Terminal UX | Done | Make local ocli feel alive and obvious to operate | Animated Oases terminal mark, auto-open Web, stable CI output | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 9. Search/read tool parity | Done | Move closer to `ocli-test` Glob/Grep/Read usefulness | `glob_files`, regex/type/output grep modes, line-range numbered reads | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 10. Sub-agent v1 | Done | Bring first `ocli-test` AgentTool-style delegation into Oases protocol | `agent_run` tool, bounded nested agent loop, subagent events, smoke coverage | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 11. Background sub-agent v1 | Done | Add first `ocli-test`-style background delegation loop | `agent_run(runInBackground)`, `agent_status`, background subagent events | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 12. Worktree sub-agent isolation v1 | Done | Let delegated agents modify an isolated git worktree without polluting the main workspace | `agent_run(isolation:"worktree")`, detached worktree creation, worktree status return, isolation smoke | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, syntax checks passed | Local runtime only |
| 13. Worktree lifecycle tools v1 | Done | Let agents inspect, apply, and remove isolated worktree outputs | `worktree_list`, `worktree_diff`, `worktree_apply`, `worktree_remove`, path validation, apply/remove guards | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, syntax checks passed | Local runtime only |
| 14. Web worktree control surface v1 | Done | Surface isolated worktree outputs in Oases Web project mode | Session detail worktree extraction, inspect/apply/remove buttons, local UI verification | `pnpm build`, `pnpm test:ocli`, `pnpm test:ocli-package` passed | Preview deployed |
| 15. Workspace custom agents v1 | Done | Let projects define reusable local sub-agents inspired by `ocli-test` AgentTool definitions | `agent_list`, `agent_read`, `agent_run(agentName)`, custom prompt injection, smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 16. Custom agent tool scoping v1 | Done | Enforce `ocli-test`-style tools/disallowedTools boundaries for workspace custom agents | Scoped OpenAI tool schemas, execution-time tool boundary checks, restricted-agent smoke | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 17. Custom agent skill preloading v1 | Done | Preload workspace skills declared by custom agent frontmatter before the first sub-agent model turn | `skills` frontmatter preload, `<skill_context>` injection, `skill_loaded` events, smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 18. Custom agent initialPrompt v1 | Done | Bring `ocli-test` custom agent first-turn prompt seeding into Oases custom agents | `initialPrompt` frontmatter parsing, first user-turn prepend, result metadata, smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 19. Agent frontmatter YAML compatibility v1 | Done | Accept common `ocli-test`/Claude-style Markdown frontmatter forms for custom agents | YAML `- item` lists for tools/skills, `initialPrompt: \|` block scalars, inline arrays, smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 20. Custom agent effort v1 | Done | Bring `ocli-test` custom agent effort frontmatter into Oases sub-agent requests without local model ownership | `effort` frontmatter parsing, sub-agent reasoning effort override, metadata, smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local runtime only |
| 21. Standalone CLI push folder | Done | Give the user a clean folder that can be pushed as a separate GitHub/npm package source | `oases-ocli/` with package manifest, bin, runtime source, standalone smoke tests, docs, ignore files; README push guidance | `cd oases-ocli && npm test`, root `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` | Local docs/package structure only |
| 22. Zero-argument CLI startup | Done | Make the published terminal experience start with `ocli` instead of `ocli serve --workspace .` | CLI parser defaults to `serve`, help/docs promote `ocli`, package smoke covers zero-arg and leading-flag forms | `cd oases-ocli && npm test`, root `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed | Local CLI UX only |
| 24. Workspace absolute paths and oases prompt | Done | Fix Web/CLI disagreement where `/Users/qingteng/Downloads` was wrongly treated as outside `workspace: /Users/qingteng` | `workspaceRelativePath`, file tool absolute-path normalization, Web project prompt from `oasesprompt.md`, Web pre-validation bypass for connected ocli, `0.1.1` package bump | `pnpm test:ocli`, `pnpm test:ocli-package`, `cd oases-ocli && npm test`, `pnpm build`, npm registry/local install checks passed | Published `oases-ocli@0.1.1`; Web production deployed |
| 25. Self-update command | Done | Let users upgrade with `ocli update` instead of remembering npm syntax | CLI parser update command, `src/updater.js`, entrypoint wiring, help/docs, package smoke dry-run coverage, `0.1.2` package bump | `pnpm test:ocli-package` and `cd oases-ocli && npm test` passed | Published by user |
| 26. Windows terminal compatibility | Done | Stop animated terminal UI from repeatedly refreshing legacy Windows cmd windows | Terminal capability detection, Windows cmd static fallback, Windows Terminal/VS Code animation allowlist, `OCLI_ANIMATED_UI=1` override, `0.1.3` package bump | `node --check`, `pnpm test:ocli-package`, `pnpm test:ocli`, `cd oases-ocli && npm test`, `npm view oases-ocli@latest`, and local global install passed | Published `oases-ocli@0.1.3` |
| 27. Web ocli onboarding v2 | Done | Make project-mode ocli onboarding usable for programming beginners and surface stale CLI versions | Windows/Mac/Linux setup tabs, click-to-copy command buttons, connected-version comparison against npm latest, update command guide, mobile no-install-command guard | `pnpm build`, local Playwright desktop disconnected screenshot, mocked stale-version update screenshot, command copy smoke, and mobile no-install-command smoke passed | Preview deployed |
| 28. Web onboarding polish and permission noise reduction | Done | Make project mode friendlier for beginners while reducing unnecessary local approvals | Node.js check/install commands, bounded project-workspace modal scrolling, credible Oases model labels, absolute OG/Twitter images, hidden empty Recents, clickable WebGL intro page, `0.1.4` approval policy that prompts for destructive tools and file-modifying shell/Python commands while allowing read commands | `node --check`, `pnpm build`, `pnpm test:ocli-package`, `pnpm test:ocli`, `cd oases-ocli && npm test`, Playwright QA, and Vercel production inspection passed | Production deployed: `https://www.oasesai.xyz` / `https://oases-chat-dejzq2y29-qingtengs-projects.vercel.app` |
| 29. Review hardening slice | Done | Address high-priority review findings without expanding into large storage/refactor work | True incremental SSE reading in ocli agent loop, SSRF-hardened `fetch_url` with manual redirects and actual-socket DNS checks, bounded response reads, default approval for agent shell/Python execution, symlink-aware workspace path checks, debounced conversation persistence, non-janky streaming scroll, `0.1.5` package prep | `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Published `oases-ocli@0.1.5`; Web changes pending deploy |
| 30. Default local token auth | Done | Close the loopback unauthenticated-control gap while keeping beginner setup usable | ocli now always generates/requires a token, terminal prints token and tokenized Web link, `/health` unauthenticated returns bounded `authRequired`, all Web local ocli requests send bearer token, project-mode token entry/save/clear UI, `0.1.6` package prep | `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, `pnpm build`, local HTTP auth smoke, and Vercel production inspection passed | Web production deployed; npm publish pending |
| 31. Tokenized Web reopen command | Done | Let users recover the current workspace Web/token connection without manually copying terminal token | Runtime info file under `.oases/ocli/runtime.json`, `ocli open`, `ocli open --dry-run`, docs and smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | `oases-ocli@0.1.6` is npm latest |
| 32. Web storage, bundle, tests, and repo cleanup | Done | Finish the next tranche of review items without risky large refactors | IndexedDB conversation persistence with legacy localStorage migration, markdown renderer lazy chunk, unit-test script and pure parser/semver tests, e2e IndexedDB fixture update, root debug/data artifact archive | `pnpm test:unit`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, `pnpm build`, and focused Playwright model-switch e2e passed | Preview deployed |
| 33. Public Oases model spec | Done | Align Web model picker, defaults, mode routing, and docs with the public Oases model names | Text/Image/Voice/Project model table, Oases Ultra local daily counter, project-mode-specific Oases Project label for `mimo-v2.5-pro`, stale model redirects, updated e2e expectations | `pnpm test:unit`, focused Playwright model spec suite, `pnpm build`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Production deployed |
| 34. Web polish, intro, and legal pages | Done | Fix current Web visual bugs and make public product/legal pages production-ready | Welcome greeting clipping fix, image-generation loader z-index fix, light-mode transition fix, multilingual Oases intro page, real Features/Models/Company/Legal links, new Terms page | `pnpm test:unit`, `pnpm build`, Browser desktop/mobile QA passed | Preview deployed |
| 35. Document disguise mode | Done | Let users temporarily disguise the chat surface as a document editor while preserving chat input/output | Top-toolbar document toggle, Word-like dark editor shell, ribbon/status/document page, inline composer, static role markers, real document zoom, no document-mode auto-stick-to-bottom, mobile responsive polish | `pnpm test:unit`, `pnpm build`, Browser desktop/mobile QA passed | Preview deployed |
| 36. Real document editor v1 | Done | Turn document disguise from a visual shell into a usable document editor | `contentEditable` document body, per-conversation document drafts, real formatting commands, message-to-document append sync, Word-compatible `.doc` export, `.html` export, mobile export access | `pnpm test:unit`, `pnpm build`, Playwright desktop/mobile export checks passed | Preview deployed |
| 37. Word-style AI editing disguise v2 | Done | Make the document mode read as AI-assisted Word editing rather than chat inside a Word shell | Chat role labels removed; user turns render as document review notes/edit instructions; assistant turns render as Oases revision blocks; active assistant blocks update during streaming; multi-tab Ribbon adds real file/insert/layout/review/view commands; ruler/page layout/print polish added | `pnpm test:unit` and `pnpm build` passed; Browser desktop QA passed for existing-conversation rendering; Playwright desktop/mobile layout and insert-table checks passed | Preview deployed |
| 38. Full Word paragraph disguise v3 | Done | Remove all visible AI/product instruction language from document mode and make the conversation appear as ordinary Word paragraphs | User and assistant turns render as unlabeled paragraphs with subtle left revision bars; the bottom composer becomes an unlabeled in-document paragraph line; Enter submits the paragraph and the next paragraph auto-generates the model reply; empty documents no longer show a body title | `pnpm test:unit`, `pnpm build`, Playwright desktop mocked-submit check, and Playwright mobile layout check passed | Preview deployed |
| 39. Word paragraph loader restore | Done | Restore the original model waiting animation inside the full Word disguise without adding visible product/instruction text | Empty assistant paragraphs render a no-label MatrixLoader-style 4x4 point animation; the loader is replaced by generated paragraph text as soon as streaming content arrives | `pnpm test:unit`, `pnpm build`, Playwright delayed-stream loader check passed | Preview deployed |
| 40. Word current-paragraph input v4 | Done | Remove the remaining textarea-like composer feel from document disguise mode | The active input line is now a real `contentEditable` document paragraph, not a textarea; the hidden submit control remains invisible, Enter submits, Shift+Enter keeps multiline editing, the prompt clears after generation, focus returns to the current paragraph, and Word-like shortcuts support save/print/zoom | `pnpm test:unit`, `pnpm build`, Playwright desktop delayed-stream/contentEditable/zoom check, and Playwright mobile no-overflow check passed | Preview deployed |
| 41. Word document draft persistence | Done | Make manual Word document edits survive refresh/reopen instead of existing only in runtime refs | Added IndexedDB-backed `document-drafts:v1` records keyed by conversation id, persisted HTML draft + synced message count + timestamp, restored drafts when entering document mode, cleaned internal placeholder attributes before persistence/export, and removed drafts when conversations/history are removed | `pnpm test:unit`, `pnpm build`, Playwright IndexedDB refresh-restore check, and mobile no-overflow check passed | Preview deployed |
| 42. Word find and replace v1 | Done | Turn the titlebar search affordance into a real document editing feature | Added a Word-style find/replace panel, `Cmd/Ctrl+F` and `Cmd/Ctrl+H` shortcuts, match highlighting, previous/next navigation, replace-all editing, mobile Ribbon access, and HTML cleanup so search highlight marks never persist into drafts or exports | `pnpm test:unit`, `pnpm build`, Playwright find/replace/persistence check, and mobile panel no-overflow check passed | Preview deployed |
| 43. Word inline image insertion | Done | Add real document-level local image insertion instead of treating images only as chat attachments | Added an Insert > 图片 command with a dedicated hidden image file input, 6MB client-side image limit, data URL figure insertion with caption, editor/export figure styling, draft persistence, refresh restore, and HTML export coverage | `pnpm test:unit`, `pnpm build`, Playwright image insert/persist/export check, and mobile Insert tab no-overflow check passed | Preview deployed |
| 44. Unified model waiting loader | Done | Restore the original model waiting animation across normal chat and the Word document disguise | Shared pending-message detection, MatrixLoader rendering for model/voice/ocli continuation waits, Word document body maps waiting placeholders to the same no-label 4x4 point animation instead of visible placeholder text | `pnpm test:unit`, `pnpm build`, Browser base check, and focused Playwright delayed-loader checks passed | Preview deployed |
| 45. Word image formatting v1 | Done | Make inserted document images editable instead of static embedded figures | Selectable document images with stable `data-doc-image-id`, contextual Ribbon image-format tools, small/medium/large sizing, left/center/right alignment, delete-image command, clean draft/export behavior that preserves formatting but strips transient selection | `pnpm test:unit`, `pnpm build`, Playwright desktop image format/persist/export/delete check, and mobile toolbar/no-overflow check passed | Production deployed |
| 46. Bundled OcliSkills v1 | Done | Make npm-installed `ocli` closer to Codex Code by shipping built-in skills and loading them automatically when relevant | Multi-source `skill_list` / `skill_read` for workspace `.oases/skills` plus packaged `OcliSkills`, skill context source metadata, package tarball inclusion, smoke coverage for installed bundled skill reads, `0.1.7` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Local CLI/package change only |
| 47. Skill asset resources v1 | Done | Let loaded skills expose their own references, scripts, evals, and examples without opening arbitrary package paths | `skill_asset_list`, `skill_asset_read`, skill-directory realpath boundary checks, package smoke for installed bundled asset reads, audit metadata preserves skill source/root | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Local CLI/package change only |
| 48. Bundled skill install v1 | Done | Let projects localize bundled skills for team customization, closer to Codex-style installed skills | `skill_install`, safe target-name validation, no-overwrite install into `.oases/skills`, recursive copy without symlinks, smoke coverage for installed package skill install | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Local CLI/package change only |
| 49. Workspace plugin manifests v1 | Done | Bring Claude/Oases-style plugin manifests into ocli discovery so commands/agents/hooks can be wired in later | `plugin_list`, `plugin_read`, `.oases-plugin` and `.claude-plugin` manifest support, README/commands/agents/hooks summaries, smoke/package coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Local CLI/package change only |
| 50. Plugin command templates v1 | Done | Let `ocli` read workspace plugin command Markdown before any plugin execution layer exists | `plugin_command_list`, `plugin_command_read`, frontmatter/heading metadata, path boundary checks, agent result summaries, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 51. Plugin agent definitions v1 | Done | Let `ocli` inspect plugin-provided agents from `ocli-test`-style plugin folders | `plugin_agent_list`, `plugin_agent_read`, shared custom-agent frontmatter parsing, source/plugin metadata, path boundary checks, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 52. Plugin skill definitions v1 | Done | Let `ocli` inspect plugin-provided skills from `ocli-test`-style plugin folders | `plugin_skill_list`, `plugin_skill_read`, plugin manifest skill summaries, skill frontmatter parsing, path boundary checks, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 53. Plugin asset resources v1 | Done | Let `ocli` safely read plugin-owned references, scripts, examples, hooks, and nested resources | `plugin_asset_list`, `plugin_asset_read`, plugin-root realpath boundary checks, 512KB read cap, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 54. Workspace plugin install v1 | Done | Let projects import local plugin folders into `.oases/plugins` without executing plugin code | `plugin_install`, manifest validation, no-overwrite copy, symlink/.git/node_modules skip, installed-plugin discovery/read smoke, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 55. Workspace plugin remove v1 | Done | Let projects safely remove installed workspace plugins as part of a full plugin lifecycle | `plugin_remove`, manifest validation before deletion, `.oases/plugins` realpath boundary checks, destructive approval policy, agent result summary, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 56. Workspace plugin enable/disable v1 | Done | Let projects temporarily disable installed plugins without deleting them | `plugin_disable`, `plugin_enable`, `.oases-disabled` marker, plugin enabled/disabled metadata, default disabled-plugin filtering for command/agent/skill discovery, `includeDisabled` opt-in, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 57. Plugin skill install v1 | Done | Let plugin-provided skills graduate into normal project skills for agent use and team customization | `plugin_skill_install`, plugin skill directory copy into `.oases/skills`, no-overwrite target validation, copied asset verification, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 58. Plugin agent install v1 | Done | Let plugin-provided agents graduate into normal workspace custom agents for `agent_run(agentName)` | `plugin_agent_install`, plugin agent Markdown copy into `.oases/agents`, no-overwrite target validation, workspace agent read/list verification, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 59. Plugin command install v1 | Done | Let plugin-provided commands graduate into normal workspace command templates for project reuse | `command_list`, `command_read`, `plugin_command_install`, no-overwrite target validation, workspace command read/list verification, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 60. Plugin hook discovery v1 | Done | Let `ocli` inspect plugin hook configs and handlers before any hook execution layer exists | `plugin_hook_list`, `plugin_hook_read`, hooks-handlers summaries, `hooks.json` event/matcher/command parsing, disabled-plugin filtering, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 61. Plugin manifest capabilities v1 | Done | Let `ocli` inspect richer plugin manifest declarations before MCP/LSP/settings execution exists | `plugin_capability_list`, `plugin_capability_read`, MCP/LSP/settings/path/commandsMetadata summaries, output-styles/settings.json discovery, sensitive setting redaction, disabled-plugin filtering, root and standalone package smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 62. Plugin output styles v1 | Done | Let plugin-provided output styles become normal workspace style templates before automatic style application exists | `output_style_list`, `output_style_read`, `plugin_output_style_list`, `plugin_output_style_read`, `plugin_output_style_install`, frontmatter/heading parsing, no-overwrite install validation, disabled-plugin filtering, `0.1.8` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 63. Explicit output style loading v1 | Done | Let explicitly read output styles guide later model turns in the same agent session | `output_style_read` / `plugin_output_style_read` session activation, `<output_style_context>` injection, `output_style_loaded` audit events, `activeOutputStyles` result metadata, preloaded output style support, root and standalone smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package change only |
| 64. Workspace settings discovery v1 | Done | Give `ocli` a safe read-only project settings layer before applying settings, permissions, MCP, or hooks | `settings_list`, `settings_read`, `.oases/settings*.json` discovery, opt-in `.claude/settings*.json` compatibility audit, sensitive setting redaction, `0.1.9` package prep, root and standalone smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 65. Settings outputStyle activation v1 | Done | Bring the first safe `ocli-test` settings application behavior into Oases agent sessions | `.oases/settings*.json` `outputStyle` whitelist, settings.local override precedence, automatic workspace/plugin output style preload before first model turn, `output_style_loaded(settings:true)` audit metadata, `0.1.10` package prep, root and standalone smoke coverage | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 66. Settings permissions deny v1 | Done | Add the first safe project permissions setting without weakening local execution safety | `.oases/settings*.json` `permissions.deny` parsing, legacy rule aliases such as `Write(...)`/`Bash(...)`, tool-wide and content-fragment hard blocks, schema filtering for tool-wide denies, `settings_permissions_loaded` audit events, `settingsPermissions` result metadata, `0.1.11` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 67. Settings permissions ask v1 | Done | Let project settings add explicit approval checkpoints without silently granting broader permissions | `.oases/settings*.json` `permissions.ask` parsing, legacy rule aliases such as `Write(...)`/`Bash(...)`, forced approval before matching tool execution, `settings_permission_ask` approval category, `settings_permissions_loaded` ask metadata, `settingsPermissions.askCount/askedTools/toolWideAsk`, `0.1.12` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 68. Local settings permissions allow v1 | Done | Let users locally reduce repeated approval prompts without letting committed project settings silently broaden permissions | `.oases/settings.local.json` `permissions.allow` parsing, legacy rule aliases such as `Bash(...)`/`Python(...)`, default-approval skip for matching calls after deny/ask precedence, `settings_permission_allowed` audit events, `settingsPermissions.allowCount/allowedTools/toolWideAllow`, `0.1.13` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 69. Settings permissions defaultMode safe subset v1 | Done | Bring the restrictive parts of `ocli-test` default permission modes into Oases without enabling bypass-style modes | `.oases/settings*.json` `permissions.defaultMode` parsing, support for `default`/`plan`/`dontAsk`, plan-mode blocking for write/execution/destructive/agent tools, dontAsk denial for unapproved approval-required calls, unsupported-mode `settings_warning`, `settingsPermissions.defaultMode/defaultModePath`, `0.1.14` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 70. Explicit command template loading v1 | Done | Let explicitly read command templates guide later model turns in the same agent session | `command_read` / `plugin_command_read` session activation, `<command_context>` injection, `command_loaded` audit events, `activeCommands` result metadata, sub-agent active command propagation, `0.1.15` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 71. Preloaded command templates v1 | Done | Let Web-selected commands and custom agents activate command templates before the first model turn | `preloadedCommands` session input, custom agent `commands` frontmatter parsing, workspace/plugin command preload before sub-agent turns, `command_loaded(preloaded:true)`, active command propagation, `0.1.16` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed | Local CLI/package/docs change only |
| 72. Web command template picker v1 | Done | Let desktop Web project mode select workspace/plugin command templates before starting an ocli agent session | Web `command_list` / `plugin_command_list` discovery, composer/workspace command selector, selected `command_read` / `plugin_command_read`, `preloadedCommands` for `/agent/sessions` and `/agent/run`, project prompt/tool rendering updates | `pnpm build` passed | Web/docs change only; not deployed yet |
| 73. Web output style picker v1 | Done | Let desktop Web project mode select workspace/plugin output styles before starting an ocli agent session | Web `output_style_list` / `plugin_output_style_list` discovery, composer/workspace output style selector, selected `output_style_read` / `plugin_output_style_read`, `preloadedOutputStyles` for `/agent/sessions` and `/agent/run`, wrapping project workbench strip | `pnpm build` passed | Web/docs change only; not deployed yet |
| 74. Project memory tools v1 | Done | Bring a conservative Codex/Claude-style project memory file layer into ocli without automatic memory injection yet | `memory_list`, `memory_read`, `memory_write`, `.oases/memory/{project,team,private}/*.md` single-level path policy, metadata/frontmatter parsing, no-overwrite default, `0.1.17` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `npm pack --dry-run` passed | Local CLI/package/docs change only |
| 75. Explicit memory loading v1 | Done | Let explicitly selected project memory guide later agent turns without enabling automatic bulk memory injection | `memory_read` session activation, `<memory_context>` injection, `memory_loaded` audit events, `activeMemories` result metadata, `preloadedMemories` session input, custom agent `memories` frontmatter, `0.1.18` package prep | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `npm pack --dry-run` passed | Local CLI/package/docs change only |
| 76. Web memory picker | Done |
| 77. Native web_search tool | Done |
| 78. todo_read + todo_write persistence
| 79. MCP client bridge v1 | Done | Add zero-dependency MCP (Model Context Protocol) client using JSON-RPC over stdio, matching ocli-test MCPTool infrastructure | `mcpClient.js` with stdio JSON-RPC transport, `mcp_list` tool, `mcp_call` tool, `mcp_resources_list` tool, `mcp_resource_read` tool, `PROJECT_TOOL_NAMES` update, Web UI type/routing/risk/success/system prompt, synced to `oases-ocli/` | `pnpm build` passed | Production deployed |
| 80. Auto MCP memory evidence | Done | Preserve auto-routed MCP calls in self-updating project memory so later RAG can recover the real external context used by an agent session | `capabilityRouting.autoMcpResults` now feeds memory maintenance, generated memory Markdown includes `MCP Evidence`, evidence metadata records `mcpResultCount`, Web memory cards surface MCP evidence counts, root and standalone smoke assertions cover `docs/search_docs` result preservation | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, `pnpm test:unit`, `pnpm build`, focused Playwright, production HTTP 200, and Vercel runtime-error check passed | Production deployed `dpl_ALHWkshfpvM8UcWLzgAvVDjBJ4H5` |
| 81. Context compaction MCP result snapshot | Done | Keep auto-routed MCP result evidence alive across 90% context compaction so long-running agents can continue from a compressed state without losing external-source facts | Context state snapshots now include compacted `autoMcpResults`, resume rules explicitly preserve MCP result evidence, root and standalone smoke add a combined auto-MCP + context-compaction session that verifies `docs/search_docs` result text survives into the compacted model request | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed | Local CLI/package change only; no Vercel deploy required |
| 82. Custom agent MCP tool scoping | Done | Let reusable custom agents constrain `mcp_call` by MCP server/tool, matching the mature Agent boundary expected for framework-driven MCP workflows | Custom agent metadata now parses/persists `mcpTools` and `disallowedMcpTools`; `agent_write` exposes both fields; `agent_run(agentName)` forwards MCP allow/deny scope into sub-agent execution; `mcp_call` is blocked before transport when a sub-agent calls a denied or non-allowed `server/tool`; agent context/session metadata preserve the scope for audit | `node --check`, `pnpm test:ocli`, `npm test --prefix oases-ocli`, `pnpm test:ocli-package`, and `pnpm build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 83. Agent Framework MCP dependency injection | Done | Make framework-declared MCP tools real runtime capabilities instead of metadata-only routing hints | Framework `mcpTools` and `mcpServers` now resolve through `mcp_list`; auto-routed framework MCP tools merge into `capabilityRouting.selected.mcpTools` and `<mcp_context>`; explicit/preloaded framework reads inject MCP context and emit `mcp_context_loaded`; root and standalone smoke cover `docs/search_docs` framework dependency injection | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, and `npm run build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 84. Agent Framework MCP resource dependencies | Done | Let frameworks declare MCP resources as first-class runtime context alongside tools | `agent_framework_write/read/list` now persist `mcpResources`; framework `mcpResources` resolve through `mcp_resources_list`; framework `mcpServers` preload matching tools and resources; auto-routed/preloaded/explicit frameworks inject resource metadata into `<mcp_context>`, `mcp_context_loaded`, `capabilityRouting.selected.mcpResources`, active framework metadata, and context-compaction snapshots | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, and `npm run build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 85. Auto-memory routing evidence | Done | Preserve framework and MCP routing decisions in self-updating wiki/RAG memory, even when MCP resources were only loaded as context and not read | Auto memory suggestions now include a `Routing Evidence` section for selected frameworks, MCP tools, and MCP resources; suggestion links include framework and MCP resource targets; evidence metadata records framework/tool/resource counts; root and standalone smoke verify `research-stack` and `docs://routing-guide` survive in memory suggestions | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, and `npm run build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 86. Web request policy and Framework MCP surface | Done | Fix Web chat request compatibility for Kimi/GPT models and expose Framework MCP dependencies in project-mode UI | Web request policy now sends Kimi with `temperature=1` and no effort fields, keeps GPT fixed temperature, preserves Project model request policy without stale text-model redirects, Vercel proxy normalizes stale Kimi/Gemini requests, and Framework summaries show MCP servers/tools/resources in composer and workspace panel | `npm run test:unit`, `npm run build`, focused Playwright Kimi/GPT request-policy tests, Browser QA with an ocli mock, production HTTP 200, production Kimi stale-request smoke, and runtime-error check passed | Production deployed `dpl_DjD7Xf7iWqdpy7LnbLBgEknogzZd` |
| 87. Adaptive context compaction tail shrinking | Done | Make 90% context compaction robust when the recent message tail is itself too large | Context compaction now tries progressively smaller recent-tail windows until the estimated next request falls below the threshold, records requested vs actual retained message counts, exposes `adaptiveRetainedMessageCount` and `overThresholdAfterCompaction`, and preserves those fields in session result metadata; root and standalone smoke include an oversized recent-tail regression | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, and `npm run build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 88. Web compaction telemetry surface | Done | Surface adaptive context compaction metadata in Web engineering session detail | Web session detail now preserves and renders requested vs actual retained message counts, adaptive tail shrinking, over-threshold status, token deltas, threshold, compacted message count, and state snapshot badges from ocli context-compaction events/results | `npm run test:unit`, `npm run build`, Browser QA with an ocli mock, production HTTP 200, production stale Kimi request smoke, and runtime-error check passed | Production deployed `dpl_3PLNoh1cgSk1yua3vEdCiABVw9FM` |
| 89. Agent Framework execution blueprint | Done | Turn framework metadata into an explicit multi-agent orchestration contract | Agent Frameworks now persist `agentRoles`, `handoffs`, and `verificationGates`; routing uses those fields for matching; framework context injects a `<framework_execution_blueprint>` that tells the orchestrator when to call `agent_run`, how to hand off between roles, and what gates must pass before final response; active framework metadata, context-compaction snapshots, and auto-memory/RAG suggestions preserve the blueprint evidence; package/runtime version prepared as `0.1.19` | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, and `npm run build` passed | Local CLI/package/docs change only; no Vercel deploy required |
| 90. Web Agent Framework blueprint surface | Done | Make Framework execution blueprints visible and traceable in Web engineering mode | Web Framework templates, preloaded session payloads, automatic capability-routing events, loaded-framework events, session capability details, tool previews, memory evidence badges, and project-mode prompt now preserve and surface `agentRoles`, `handoffs`, and `verificationGates` | `npm run test:unit`, `npm run build`, focused desktop Playwright coverage for Framework preload/routing/session detail, production HTTP 200, production JS asset marker check, and Vercel runtime-error scan passed | Production deployed `dpl_G3T9xX9RvMRQ3jbqzBSg7VygNMnf` |
| 95. Persistent todo restore | Done | Keep long Ocli jobs from forgetting open `.oases/todo.json` work across resumed or fresh sessions | Agent startup reads persisted todos, injects only open todo context, emits `todo_state_loaded`, preserves completion guard auto-continuation, Web renders todo restore/update telemetry, and session detail scrolls opened details into view | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, `npm run test:unit`, `npm run build`, focused desktop Playwright checks, production HTTP 200, production JS marker check, and Vercel runtime-error scan passed | Production deployed `dpl_FWR2LoDrHYksWu6ebCUYc7pn6iEX` |
| 96. Todo-aware context compaction and session detail | Done | Ensure restored todo state survives 90% context compaction and remains visible in Web session audits | Compaction now receives `latestTodoState`, state snapshots preserve `openTodos/latestTodos`, smoke verifies restored todos survive compaction and force auto-continuation, and Web session detail merges `result.latestTodos/openTodos/todoCounts` when top-level todos are empty | `node --check`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:ocli-package`, `npm run test:unit`, `npm run build`, focused desktop Playwright checks, production HTTP 200, production JS marker check, and Vercel runtime-error scan passed | Production deployed `dpl_B5Rhh2zTWK8ZjZFjHeqewrcLqM3b` |
| 102. Capability routing diagnostics | Done | Make automatic MCP/skill/memory/agent/framework selection auditable across Web, compaction, and resume | `capabilityRouting.diagnostics` records query terms, candidate counts, selected counts, thresholds, and top selected/rejected samples; SSE routing events include diagnostics; Web session detail renders a Routing diagnostics panel; 90% context snapshots and resume payloads preserve diagnostics; session resume/context-state tags now compact fields while preserving valid JSON; package version prepared as `0.1.22` | `node --check`, `npm run test:ocli`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, and focused desktop Playwright session-detail check passed | Production deployed `dpl_AtK4C2dxEn1HKFMx9hctbSVWehgQ` |
| 103. Todo-aware auto-memory continuity | Done | Make self-updating wiki/RAG memory preserve unfinished work and continuation state when Ocli stops at a turn boundary | Auto-memory suggestions now include `Continuation State`, todo evidence, open todo evidence, and context compaction evidence; evidence metadata records todo/open-todo/compaction/auto-continuation counts plus `stoppedReason`; sessions preserve `memoryMaintenance` in resume payloads; Web session detail renders open/compaction/continuation/stopped badges | `node --check`, `npm run test:ocli`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, focused desktop Playwright, full `npm run test:e2e`, production HTTP 200, deployed JS marker check, production Kimi smoke, and Vercel runtime-error/status checks passed | Production deployed `dpl_3ji2D1JBhguA1isKghkHtnVoe2Jz` |
| 104. Sub-agent evidence memory continuity | Done | Make delegated sub-agent conclusions survive auto-memory, compaction, resume, and Web session audit flows | `agent_run` and `agent_status` results now compact into `Sub-agent Evidence` memory suggestions, context snapshots, resume context, and Web session detail; evidence metadata records `subAgentCount`; Web renders a Sub-agents panel and memory badge | `node --check`, `npm run test:ocli`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, Browser page-health QA, focused desktop Playwright, full `npm run test:e2e`, production HTTP 200, deployed JS marker check, production Kimi smoke, and Vercel inspect/log checks passed | Production deployed `dpl_Cq4fbRwc3zgNyLk6CQt2GRHYrmyn` |
| 105. Adaptive routing live telemetry | Done | Make long-running Web engineering sessions show when Ocli re-routes skills, memory, and MCP from later tool/output context | Live project timeline now distinguishes initial automatic routing from adaptive routing, adaptive skill loads, adaptive memory RAG searches, adaptive MCP context loads, and adaptive MCP auto-calls | `npm run build`, Browser page-health QA, focused desktop Playwright, `npm run test:unit`, full `npm run test:e2e`, production HTTP 200, deployed JS marker check, production stale Kimi request smoke, and Vercel inspect/log/error checks passed | Production deployed `dpl_AzJrLppyCSmxDfGsubUnRoXsS9ps` |
| 106. External context adaptive routing | Done | Make search and fetched web/document context participate in Ocli's adaptive capability routing | `fetch_url` and `web_search` tool results now trigger post-tool adaptive routing, while adaptive routing queries are narrowed to routing-safe context results to avoid same-turn todo/write/execution noise selecting unrelated capabilities | `node --check`, focused adaptive routing unit test, full unit tests, root smoke, package smoke, standalone package test, package dry-run, and build checks passed | Local CLI/package/docs change only; no Vercel deploy required |
| 107. Settings capability routing policy | Done | Let each project tune Ocli's automatic skill, memory, MCP, agent, and framework routing from settings | `.oases/settings.json` and `.oases/settings.local.json` now support `capabilityRouting` for enabled/adaptive/includeAgents/autoMcpCalls, selection limits, discovery limits, memory RAG search limits, and minimum scores; routing diagnostics and final session metadata preserve the active policy; `settings_read`/`settings_write` safely expose and persist the policy | `node --check`, focused routing policy unit test, full unit tests, root smoke, package smoke, standalone package test, package dry-run, and build checks passed | Local CLI/package/docs change only; no Vercel deploy required |
| 109. Settings context compaction policy | Done | Let each project tune Ocli's automatic context compaction defaults from settings | `.oases/settings.json` and `.oases/settings.local.json` now support `contextCompaction` as a boolean or `{ enabled, maxContextTokens, ratio, recentMessages }`; request bodies can still override or disable compaction; settings events, compaction event policy, context-state snapshots, final session metadata, `settings_read` safeValues, and `settings_write` allowlists preserve the active policy; package/runtime version prepared as `0.1.25` | `node --check`, root smoke, package smoke, standalone package test, unit tests, production build, and package dry-run passed | Local CLI/package/docs/Web prompt change only; no Vercel deploy required |
| 110. Web context compaction policy telemetry | Done | Make project-level context compaction settings visible in Web engineering mode during live runs and session audits | Web now normalizes `settingsContextCompaction` plus `contextCompactions[*].policy`, renders a `Context policy` session-detail panel with settings sources, max tokens, threshold, ratio, and retained-message defaults, shows `settings_context_compaction_loaded` SSE events as project tool context cards, and includes policy details in `context_compacted` previews | `npm run build`, `npm run test:unit`, focused desktop Playwright routing/compaction + session-detail checks, production HTTP 200, deployed JS marker check, and Vercel deployment inspection passed | Production deployed `dpl_Hy7KvKxsMQiSUgPWJbxARnwWFZBc` |
| 111. Web context compaction settings editor | Done | Let users tune project-level Ocli context compaction policy directly from Web engineering mode | The project workbench now reads `safeValues.contextCompaction` from `.oases/settings.local.json`, shows a compact `Context compaction` editor with enable, max tokens, trigger ratio, and recent-message controls, and persists changes through `settings_write` under the `contextCompaction` allowlist | `npm run build`, `npm run test:unit`, focused desktop Playwright memory-settings regression + context-compaction settings editor checks, production HTTP 200, deployed JS marker check, and Vercel deployment inspection passed | Production deployed `dpl_3wLoKxFFucAESaKRVmCKPXgEbFJB` |
| 112. Web capability routing settings editor | Done | Let users tune Ocli's automatic skill, memory, MCP, agent, and framework routing policy from Web engineering mode | The project workbench now reads `safeValues.capabilityRouting` from `.oases/settings.local.json`, shows a compact `Capability routing` editor for routing toggles, selection limits, Memory RAG limits, and minimum score thresholds, and persists the full policy through `settings_write` under the `capabilityRouting` allowlist | `npm run build`, `npm run test:unit`, focused desktop Playwright context-compaction settings regression + capability-routing settings editor checks, production HTTP 200, deployed JS marker check, and Vercel deployment inspection passed | Production deployed `dpl_6UwYfRDi617AQL4jafp5XHm4kDcY` |
| 113. Web memory policy settings editor | Done | Let users tune Ocli's self-updating wiki/RAG memory behavior from Web engineering mode | The project workbench now reads `safeValues.memory` from `.oases/settings.local.json`, shows a compact `Memory policy` editor for auto-suggest, auto-write, and write scope, preserves immediate auto-write toggling, and persists the full memory policy through `settings_write` under the `memory` allowlist | `npm run build`, `npm run test:unit`, focused desktop Playwright memory-policy/context-compaction/capability-routing settings checks, production HTTP 200, deployed JS marker check, Vercel deployment inspection, and 30-minute runtime-error log check passed | Production deployed `dpl_5Kk5GAyuVZgY8WmqKob3mSYPHPNU` |
| 114. Web Agent Framework composer | Done | Let users create reusable Ocli multi-agent frameworks directly from Web engineering mode | The project workbench now includes a compact Agent Framework Composer that writes `agent_framework_write` definitions with agents, skills, commands, memories, MCP servers/tools/resources, routing terms, agent roles, handoffs, and verification gates; saved frameworks are inserted into the local framework list, selected immediately, and then preloaded through the existing `agent_framework_read` session path | `npm run build`, `npm run test:unit`, focused desktop Playwright Agent/Framework/routing/settings checks, production HTTP 200, deployed JS marker check, Vercel deployment inspection, and 30-minute runtime-error log check passed | Production deployed `dpl_62oTBgkjo5hSac1SQxYLtxScWFij` |
| 115. Web MCP server settings editor | Done preview | Let users maintain project MCP server declarations from Web engineering mode so automatic MCP routing has a visible configuration surface | `settings_read.safeValues` now exposes safe MCP server summaries with command, sanitized args, args count, env key names, and no env values; `settings_write` now merges individual `mcpServers` patches so Web edits do not wipe existing secret env; the project workbench now renders an `MCP servers` panel to inspect, edit, add, and delete stdio server command/args entries before `mcp_list`, `mcp_call`, and auto MCP routing use them | `node --check`, `npm run test:ocli`, `npm run test:ocli-package`, `npm run build`, `npm run test:unit`, focused desktop Playwright Agent Framework/settings regressions + MCP server settings editor checks passed | Preview deployed; latest URL is tracked below |
| 116. Web MCP discovery surface | Done preview | Let users verify the real MCP tools and resources available to Ocli automatic routing from Web engineering mode | The project workbench adds an MCP discovery action that calls `mcp_list` and `mcp_resources_list`, then renders available tools, resources, input-schema keys, server counts, and initialization errors next to the server settings panel without persisting dynamic discovery results | `npm run build`, `npm run test:unit`, focused desktop Playwright MCP server discovery check, and focused Agent Framework/memory/routing/context-compaction/MCP settings regressions passed | Preview deployed `dpl_EHg9BDFZJuvtMfXtTqxffSUC5nzn` |
| 117. Web Memory RAG search surface | Done preview | Let users verify Ocli's wiki-style memory RAG directly from Web engineering mode before relying on automatic memory routing | The project workbench adds a `Memory RAG` panel that calls `memory_search` with query/scope and renders ranked snippets, matched terms, outgoing links, backlinks, and routing-policy search limits | `npm run build`, `npm run test:unit`, focused desktop Playwright Memory RAG check, broader Agent Framework/memory/routing/context-compaction/MCP settings regressions, and Vercel deployment inspection passed | Preview deployed `dpl_ECscurPCRRU28ZgKhmYhMkzcNDxd` |
| 118. Web/Ocli capability route preview | Done preview | Let users audit Ocli's automatic skills, memory, agent, framework, and MCP routing before starting a long engineering session | Ocli adds a read-only `capability_route_preview` tool that ranks discovered skills, commands, Memory RAG results, agents, Agent Frameworks, MCP tools, and MCP resources against a task using project routing settings; Web adds a `Route Preview` panel that passes the current routing policy draft and renders selected capabilities, scores, matched terms, candidate counts, policy sources, framework gates, and MCP schema keys | `node --check`, direct `handleTool` route-preview smoke, `npx tsc --noEmit`, `npm run build`, `npm run test:unit`, `npm run test:ocli`, `npm run test:ocli-package`, focused desktop Playwright route-preview/capability-routing checks, and broader workbench regressions passed | Preview deployed `dpl_CWoLfaYsGCBKdiMsUBoBcpbqAvR8` at `https://oases-chat-r5t6tr13c-qingtengs-projects.vercel.app` |
| 119. Preview-comparable routing snapshots | Done local | Make Route Preview and real Ocli agent routing auditable with the same compact snapshot contract | Real agent routing diagnostics now include `snapshot` with total candidates, total selected, per-category counts, selected keys, phase/turn, and a stable fingerprint; `capability_route_preview` now emits the same style of snapshot; Web session detail and Route Preview panels render fingerprints and compare targets so long sessions can be checked against previews | `node --check` for both runtime copies and smoke scripts, direct `capability_route_preview` snapshot smoke, `npx tsc --noEmit`, `npm run test:unit`, `npm run build`, `npm run test:ocli`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, focused desktop Playwright route-preview/session-detail/routing checks, and broader workbench regressions passed | Local Web/Ocli/docs change only; preview deployment pending |
| 120. Web route preview/session comparison | Done preview | Let Web engineering mode immediately detect whether real Ocli automatic routing stayed aligned with the latest Route Preview | Session detail now compares the latest real routing snapshot against the latest Route Preview snapshot, labels exact fingerprint matches as `matches latest preview`, labels mismatches as drift, and includes shared selected-key counts plus selected/candidate deltas for auditability | `npx tsc --noEmit`, `npm run test:unit`, `npm run build`, focused desktop Playwright route-preview/session-detail/automatic-routing checks, focused Route Preview regression, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed | Preview deployed `dpl_3RE2r9YBvhLGrwsFQYMNGvXWyyo7` at `https://oases-chat-8gitazp2u-qingtengs-projects.vercel.app` |
| 121. Web resume readiness and executable continuation | Done preview | Make interrupted or max-turn Ocli sessions visibly resumable instead of leaving continuation as a hidden prompt | Session detail now renders a `Resume readiness` panel that shows the source session, prompt readiness, open todo count, compaction count, auto-continuation count, file evidence, and stopped reason; the panel keeps copy-resume available and its `Continue` action calls `/agent/sessions/:id/resume`, inserts the resumed session, and opens the new session detail with `session_resumed` telemetry | `npx tsc --noEmit`, `npm run build`, `npm run test:unit`, focused session-detail resume e2e, focused desktop Playwright route-preview/session-detail/automatic-routing checks, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed | Preview deployed `dpl_8BrkHYQtCVSuey6BzQJf5GTfiUhe` at `https://oases-chat-9njth6tl3-qingtengs-projects.vercel.app` |
| 122. Web session approval blockers | Done preview | Make approval-blocked Ocli sessions actionable from persisted session detail instead of only showing approval counts | Session detail now preserves approval event id/status/category/risk/reason/arguments, derives pending blockers from `approval_required` minus `approval_resolved`, renders an `Approval blockers` panel with tool, risk, category, approval id, reason, and argument preview, and lets Web approve/reject via `/agent/sessions/:id/approvals/:approvalId` before reloading detail | `npx tsc --noEmit`, `npm run build`, `npm run test:unit`, focused desktop Playwright session-detail, approval-blocker, and live approval-card regressions passed; Browser QA with mock Ocli approval flow passed with no console warnings/errors; Vercel deployment API reports READY | Preview deployed `dpl_HxyFgpTWLpJyH6vVqcYYpUB8H8Dn` at `https://oases-chat-e1rvhx41b-qingtengs-projects.vercel.app` |
| 123. Stable pending approval detail contract | Done preview | Keep long-running Ocli approval blockers visible even when recent session events no longer include the original approval request | Ocli session detail now returns `pendingApprovals` built from live pending approval records plus persisted event recovery; live records are marked `actionable: true`/`source: live`, persisted-only records are visible but not directly actionable, Web prefers this stable field over the last-events window, and package/runtime version is prepared as `0.1.26` | `node --check`, `npx tsc --noEmit`, `node scripts/ocli-smoke.mjs`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, focused desktop Playwright approval/session-detail regressions, Vercel preview deploy, and deployment API READY inspection passed | Preview deployed `dpl_5r6fXAN1QcDy1bRjv1XLi68Ek6T1` at `https://oases-chat-opaosc4y9-qingtengs-projects.vercel.app` |
| 124. Waiting approval status surface | Done preview | Make approval-blocked Ocli sessions visible from Web status strip and session list before users open session detail | Ocli health/session summaries now include `pendingApprovalCount` and `waitingForApproval` from live and persisted metadata; Web summary shows `approval pending` and `latest waiting approval`; session rows show amber `waiting approval` and approval-count badges | `node --check`, `npx tsc --noEmit`, `node scripts/ocli-smoke.mjs`, `npm run test:ocli-package`, `npm run test:unit`, `npm run build`, focused desktop Playwright approval/session-list checks, Vercel preview deploy, protected root HTTP 200 fetch, and deployment API READY inspection passed | Preview deployed `dpl_3PU9RYRST4sDg3JhdydiHunzE4ws` at `https://oases-chat-cbrioaqng-qingtengs-projects.vercel.app` |
| 125. Continuation-needed status surface | Done preview | Make max-turn or open-todo Ocli sessions visibly resumable from health/session summaries and Web session rows | Ocli live and persisted session metadata now expose `stoppedReason`, `needsContinuation`, and health-level `continuationPendingCount`; Web status strip renders `continuation pending` and `latest needs continuation`; session rows show a blue `needs continuation` badge plus stopped reason; package/runtime version prepared as `0.1.27` | `node --check`, `npx tsc --noEmit`, `node scripts/ocli-smoke.mjs`, `npm run test:ocli-package`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, focused desktop Playwright session-detail/approval checks, Vercel preview deploy, protected root HTTP 200 fetch, and deployment API READY inspection passed | Preview deployed `dpl_6YpM5EXNmGFeUS6beYB8tqr17rbw` at `https://oases-chat-84304h92s-qingtengs-projects.vercel.app` |
| 126. Web session row quick resume | Done preview | Let users resume max-turn/open-todo Ocli sessions directly from the Web session list instead of opening detail first | Needs-continuation session rows now split into a detail button plus a stable `继续运行` action that calls `/agent/sessions/:id/resume`, shows row-level busy state, inserts the resumed session, and opens the resumed session detail; detail resume keeps its own busy state | `npx tsc --noEmit`, `npm run build`, focused desktop Playwright session-detail/quick-resume/approval checks, and Vercel deployment `dpl_HxnuryRSQ5vj1xLZNeCZtcN2ae5N` passed | Preview: `https://oases-chat-h49g2exwr-qingtengs-projects.vercel.app`; Vercel state `READY`; project preview is protected by Vercel Authentication |
| 127. Approval-blocked resume contract | Done preview | Prevent Ocli from forking long-running work while an approval blocker is still unresolved | `/agent/sessions/:id/resume` now checks stable pending approvals before running-state checks and returns a 409 state conflict with an actionable message; Web Resume readiness now treats pending approvals as `approval pending`, disables `继续运行`, and uses session summary, stable `pendingApprovals`, and event recovery as redundant blockers; package/runtime version prepared as `0.1.28` | `node --check`, `npx tsc --noEmit`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, focused desktop Playwright session-detail/quick-resume/approval checks, Vercel deployment API READY, and preview root HTTP 200 passed | Preview deployed `dpl_6pPHmNgeHWUa4ruG1PrRDgZEDkHv` at `https://oases-chat-m7idabf50-qingtengs-projects.vercel.app` |
| 128. Failed-session recovery continuity | Done preview | Treat failed Ocli sessions as recoverable continuation work instead of dead ends | Ocli live and persisted session summaries now mark failed sessions with `stoppedReason: failed` and `needsContinuation: true`; resume prompts include the source error; structured resume context carries `sourceError`; failed-session resume requests prepend a compact `<original_session_request>` so recovery keeps the original user goal without bloating normal max-turn resume flows; Web Resume readiness now falls back to session stopped reason/status for failed details and quick-resumes failed rows | `node --check`, `npx tsc --noEmit`, `npm run test:ocli`, `npm test --prefix oases-ocli`, `npm run test:unit`, `npm run build`, focused desktop Playwright failed-session/quick-resume/approval checks, Vercel deployment API READY, and preview root HTTP 200 passed | Preview deployed `dpl_7uYEX2sjdaXGQ2hhTPejY6s295tw` at `https://oases-chat-oxb5qsdem-qingtengs-projects.vercel.app` |
 + Phase 6 done | Done | Add `todo_read` tool to read `.oases/todo.json`, enhance `todo_write` to persist todos to `.oases/todo.json`, update Phase 6 to Done, bump to 0.1.19 | `todo_read` tool definition in `tools.js`, `PROJECT_TOOL_NAMES` update, Web UI type/routing/risk/success/system prompt, synced to `oases-ocli/`, `VERSION` bump to `0.1.19` | `pnpm build` passed | Production deployed |
 Add DuckDuckGo-powered web search as a first-class ocli tool, matching ocli-test WebSearchTool parity | `webSearch` function in `network.js` using DuckDuckGo HTML API (no API key), `web_search` tool definition in `tools.js`, `PROJECT_TOOL_NAMES` update, Web UI `ProjectToolName` type, `isProjectToolName` validation, risk category `search`, success message, system prompt tool lists, detailed tool description, disconnected error handler, synced to `oases-ocli/` | `pnpm build` passed | Production deployed |
 Add memory discovery, selection, and `preloadedMemories` injection to the Web project mode UI, matching command template and output style picker patterns | `memory_list` state + `useEffect` fetch, `Brain` icon memory selector (desktop workbench strip + mobile workspace panel), i18n (zh-CN + en), system prompt memory tool mentions, `<memory_context>` injection text, `preloadedMemories` wiring to session/legacy endpoints, disconnected error handler | `pnpm build` passed | Production deployed |

## Phase 1 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 1.1 Record migration plan | Done | `docs/ocli-migration-plan.md` | This file is the live handoff document. |
| 1.2 Create official package manifest | Done | `ocli/package.json` | Package identity moved to official `ocli` package root. |
| 1.3 Create official bin entry | Done | `ocli/bin/ocli.js` | Keeps `ocli` and `oases-ocli` bin behavior. |
| 1.4 Move active runtime source | Done | `ocli/src/*.js` | Old MVP runtime archived under `ocli/legacy-mvp-src`; active runtime copied to `ocli/src`. |
| 1.5 Update root entry | Done | `ocli/index.js` | Root `pnpm ocli` starts official runtime source. |
| 1.6 Update package smoke | Done | `scripts/ocli-package-smoke.mjs` | Tests `ocli/package.json`, `ocli/bin/ocli.js`, and `npm pack --dry-run` from official package root. |
| 1.7 Update ocli smoke startup | Done | `scripts/ocli-smoke.mjs` | Root `ocli/index.js` startup remains covered. Runtime identity is now `ocli`. |
| 1.8 Update docs | Done | `README.md`, `docs/ocli-npm-release.md`, `ocli/README.md` | Documents `ocli` as official package and `ocli-test/src` as migration source. |
| 1.9 Validate | Done | scripts/build | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed. |
| 1.10 Deploy | Done | Vercel | Production deployment `dpl_FGnWbaraaR3pEoJ7BJQufMLD7S3u`, aliased to `https://www.oasesai.xyz`. |

## Phase 2 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 2.1 Define Oases tool result contract | Done | `ocli/src/tools.js`, `ocli/src/agent.js`, `src/pages/Home.tsx`, tests | Tool results now preserve top-level `artifacts` and Web normalization keeps them. |
| 2.2 Add crawler-friendly artifacts | Done | `ocli/src/tools.js`, `scripts/ocli-smoke.mjs` | `write_file`, `edit_file`, and `delete_file` expose file artifacts with path/bytes/role metadata; agent results preserve artifacts. |
| 2.3 Add Todo/Task planning tool | Done | `ocli/src/constants.js`, `ocli/src/tools.js`, `ocli/src/agent.js`, `src/pages/Home.tsx`, tests | Added `todo_write` as a structured project checklist tool. Web recognizes it, previews todo status, and crawler prompts instruct models to use it for executable progress tracking. |
| 2.4 Improve WebFetch/fetch_url parity | Done | `ocli/src/network.js`, `scripts/ocli-smoke.mjs` | `fetch_url` now returns `title` and normalized `links` for HTML pages while preserving `finalUrl`, `status`, `contentType`, `text`, and truncation metadata. |
| 2.5 Improve shell/Python tool summaries | Done | `ocli/src/tools.js`, tests | `run_command` and `run_python` now snapshot workspace changes and return generated/modified file artifacts. |
| 2.6 Update prompt guidance for migrated tools | Done | `src/pages/Home.tsx` | Prompt now instructs crawler/page-analysis tasks to use `todo_write`, avoid low-value `list_files`, write files, run scripts when needed, and summarize final outputs from artifacts. |
| 2.7 Validate Phase 2 | Done | scripts/build | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed. |
| 2.8 Deploy Phase 2 | Done | Vercel | Production deployment `dpl_Gx1DFBtJ6w5V5PdnguKmhq4ETLYQ`, aliased to `https://www.oasesai.xyz`. |

## Phase 3 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 3.1 Read-only shell command policy | Done | `ocli/src/tools.js`, `ocli/src/agent.js`, `scripts/ocli-smoke.mjs` | Allows clearly read-only commands such as `pwd`, `git status`, `git diff`, `git rev-parse`, and `git log` without approval; arbitrary shell commands, Python, and destructive tools remain gated. |
| 3.2 Approval request categories | Done | `ocli/src/tools.js`, `ocli/src/agent.js`, `ocli/src/sessions.js`, `src/pages/Home.tsx`, tests | Approval events now include category, risk, and human-readable reason; Web approval cards display category/reason. |
| 3.3 Per-session approval memory | Done | `ocli/src/agent.js`, `ocli/src/sessions.js`, `scripts/ocli-smoke.mjs` | Repeated identical high-risk tool calls now reuse the current session approval via a stable approval key; approval reuse is session-local and does not persist across restarts. |
| 3.4 Stronger destructive command detection | Done | `ocli/src/process.js`, `ocli/src/tools.js`, `scripts/ocli-smoke.mjs` | Expanded destructive command detection with explainable block reasons for workspace-wide removal, destructive git operations, system/disk commands, infrastructure deletion, and risky SQL; read-only shell auto-approval now rejects chained/control-operator commands. |
| 3.5 Validate Phase 3 | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed with the known CSS minify warning. |
| 3.6 Deploy Phase 3 | Done | Vercel | Production deployment `dpl_9yWQmFGKqbsxnKzntLpo3jJe29SN`, aliased to `https://www.oasesai.xyz`. |

## Phase 4 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 4.1 Define continuation contract | Done | `ocli/src/agent.js`, `src/pages/Home.tsx`, tests | Local agent now treats `maxTurns` as a bounded slice and has a larger `maxTotalTurns` derived from `maxAutoContinuations`; maxed-out sessions report the safer `max_turns` hard-limit state. |
| 4.2 Auto-continue local agent slices | Done | `ocli/src/agent.js`, `ocli/src/sessions.js` | When tool work reaches a slice boundary, ocli emits `auto_continue`, appends a continuation prompt, and keeps running without requiring the user to send “继续”. |
| 4.3 Completion criteria prompt/tool behavior | Done | `ocli/src/agent.js`, `src/pages/Home.tsx` | If the model replies with unfinished “I will write/create/generate” text but no tool call, ocli automatically follows up and instructs it to call tools or finish with artifact paths; Web displays auto-running state from SSE. |
| 4.4 Long-running task smoke | Done | `scripts/ocli-smoke.mjs` | Added a multi-slice fake model smoke that writes ten files across slice boundaries and completes automatically. |
| 4.5 Real crawler-style smoke | Done | `scripts/ocli-smoke.mjs` | Added crawler-style smoke covering `todo_write`, `fetch_url`, generated crawler code, generated dataset file, artifact preservation, and final artifact summary. |
| 4.6 Validate Phase 4 | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed with the known CSS minify warning. |
| 4.7 Deploy Phase 4 | Done | Vercel | Production deployment `dpl_ALXijhHX9UTUkCB7GZU4BTxcf65P`, aliased to `https://www.oasesai.xyz`. |

## Phase 5 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 5.1 Session audit summary contract | Done | `ocli/src/sessions.js`, `ocli/src/server.js`, tests | Session detail now exposes artifact list, latest todo snapshot, approval counts, and a `resumePrompt` suitable for continuing from persisted context. |
| 5.2 Web session detail panel upgrade | Done | `src/pages/Home.tsx` | Project workspace session detail now shows artifacts, todo status, approval stats, recent events, and final/error text. |
| 5.3 Persisted restart recovery smoke | Done | `scripts/ocli-smoke.mjs` | Smoke restarts ocli after a crawler-style session and verifies persisted detail still exposes artifacts, todos, approval summary, and resume prompt. |
| 5.4 Manual continue/resume affordance | Done | `src/pages/Home.tsx` | Added a safe copy-resume-prompt action; it does not silently re-run old tools. |
| 5.5 Validate Phase 5 | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed with the known CSS minify warning. |
| 5.6 Deploy Phase 5 | Done | Vercel | Production deployment `dpl_BgPsPDizHReArAF4u1SX8NLkFLgm`, aliased to `https://www.oasesai.xyz`. |

## Phase 6 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 6.1 Workspace skill discovery tools | Done | `ocli/src/constants.js`, `ocli/src/tools.js`, `src/pages/Home.tsx`, tests | Added `skill_list` and `skill_read` so ocli can discover/read workspace-local `.oases/skills/**` instructions before full skill invocation exists. |
| 6.2 Prompt guidance for skills | Done | `src/pages/Home.tsx` | Project mode now tells the model to call `skill_list`/`skill_read` when tasks match local skills, without claiming a skill was executed before reading it. |
| 6.3 Skill integration smoke | Done | `scripts/ocli-smoke.mjs` | Smoke creates a workspace skill, lists it, reads its `SKILL.md`, and verifies `skill_read` rejects paths outside `.oases/skills`. |
| 6.4 Skill invocation/sub-agent design | Done | `docs/ocli-migration-plan.md`, `ocli/src/agent.js`, tests | Implemented skill invocation v1: after `skill_read`, ocli injects `<skill_context>` into the current agent session, emits `skill_loaded`, and records `invokedSkills`; all model calls still go through the Web-owned proxy. True forked sub-agent execution remains a later Phase 6 item. |
| 6.5 Validate Phase 6 first slice | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed with the known CSS minify warning. |
| 6.6 Deploy Phase 6 first slice | Done | Vercel | Production deployment `dpl_9xAowqPdNExrhFQoYmgixGdmQaBY`, aliased to `https://www.oasesai.xyz`; Phase 6 remains open for full skill invocation/sub-agent design. |
| 6.7 Forked sub-agent execution | Pending | `ocli/src/agent.js`, `ocli/src/sessions.js`, tests | Adapt the `ocli-test` forked skill/sub-agent concept to Oases protocol without local model keys; likely requires nested agent sessions or delegated turns through the same Web proxy. |
| 6.8 Deploy Phase 6 skill invocation v1 | Done | Vercel | Production deployment `dpl_EQSzr6N87tvan1MJYxsKWsyJYNMu`, aliased to `https://www.oasesai.xyz`; Phase 6 remains open for forked sub-agent execution. |

## Phase 7 Detailed Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 7.1 Strengthen npm package smoke | Done | `scripts/ocli-package-smoke.mjs` | Smoke now runs `npm pack --dry-run`, creates a real tarball, installs it into a temporary clean npm project, and runs installed `oases-ocli --help`. |
| 7.2 Public publish config | Done | `ocli/package.json` | Added `publishConfig.access = public` for the intended public `oases-ocli` package. |
| 7.3 npm install docs | Done | `docs/ocli-npm-release.md`, `ocli/README.md` | Docs now cover `npx oases-ocli`, clean tarball smoke, no local API key, session recovery fields, and skill tools. |
| 7.4 Repository metadata | Done | `ocli/package.json`, `oases-ocli/package.json`, docs | GitHub repository is `https://github.com/qingtengCHINA/oases-ocli`; package manifests now include `repository`, `bugs`, and `homepage`. |
| 7.5 License decision | Done | `ocli/package.json`, `ocli/LICENSE`, `oases-ocli/package.json`, `oases-ocli/LICENSE`, docs | User chose `Apache-2.0`; package manifests and LICENSE files now reflect that choice. |
| 7.6 GitHub validation workflow | Done | `.github/workflows/ocli-ci.yml`, `oases-ocli/.github/workflows/ci.yml` | Added monorepo CI for Web-integrated validation and standalone CLI CI for `npm test` plus `npm pack --dry-run`. npm publish workflow remains pending until license/npm token policy is confirmed. |
| 7.7 Real npm publish | Done | npm | Published `oases-ocli@0.1.0` from `oases-ocli/` under npm account `qingtengpro`; verified `npm view oases-ocli` and clean global-prefix install with `npm install -g oases-ocli`. |
| 7.8 npm patch release | Done | npm, `oases-ocli/package.json` | Published `oases-ocli@0.1.1`; `npm view oases-ocli version dist-tags bin --json` reports latest `0.1.1`, and local global upgrade now prints `ocli 0.1.1`. |
| 7.9 npm self-update release | Done | npm, `oases-ocli/package.json` | `oases-ocli@0.1.2` adds the documented `ocli update` self-update command; user completed the npm publish. |
| 7.10 Windows cmd patch release | Done | npm, `oases-ocli/package.json`, `src/terminalUi.js` | `oases-ocli@0.1.3` prevents legacy Windows `cmd.exe` from running the animated full-screen terminal renderer; `npm view` reports latest `0.1.3`, and clean global upgrade now prints `ocli 0.1.3`. |

## Phase 8 Terminal UX Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 8.1 Animated interactive status mark | Done | `ocli/src/terminalUi.js`, `ocli/src/server.js` | Added a green dot-matrix Oases metaball animation for interactive `ocli serve` sessions. The two connected blobs continuously exchange size so terminal motion indicates that ocli is still running. |
| 8.2 Stable script/CI output | Done | `ocli/src/terminalUi.js` | Non-TTY output, CI, `TERM=dumb`, `NO_COLOR`, and `OCLI_PLAIN_UI=1` keep the original plain startup logs for smoke tests and automation. |
| 8.3 User-facing terminal guidance | Done | `ocli/src/terminalUi.js`, `ocli/README.md` | Startup UI now tells users: `正在运行ocli，请打开https://www.oasesai.xyz 选择“工程模式”配合使用`. |
| 8.4 Local validation | Done | tests/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 8.5 Logo silhouette redesign | Done | `ocli/src/terminalUi.js` | Replaced the generic metaball renderer with a PNG-derived Oases silhouette mask, preserving the right-top large lobe, left-side concave waist, lower-left lobe, and narrow connector. Animation now uses subtle edge swelling and color flow over the closer silhouette. |
| 8.6 Visible lobe-flow animation | Done | `ocli/src/terminalUi.js` | Upgraded the terminal animation so the upper-right and lower-left lobes visibly exchange size through frame-by-frame coordinate warping, creating a stronger flowing/surging effect while retaining the Oases silhouette. |
| 8.7 Auto-open Oases Web | Done | `ocli/src/terminalUi.js`, `ocli/README.md` | Interactive `ocli` sessions now open `https://www.oasesai.xyz/` after 6 seconds. `OCLI_NO_AUTO_OPEN=1`, non-TTY output, CI, and script runs do not open a browser. |

## Phase 9 Search/Read Tool Parity Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 9.1 Capability gap audit | Done | `docs/ocli-migration-plan.md`, `ocli/ocli-test/src/tools/**` | Current `ocli/src` is usable for Oases project mode but does not yet match full `ocli-test` strength. Major remaining gaps include forked sub-agents, plugin/MCP runtime, IDE/LSP integration, background tasks/worktrees, richer settings, and terminal REPL UI. |
| 9.2 Glob-style file discovery | Done | `ocli/src/constants.js`, `ocli/src/tools.js`, tests | Added `glob_files` with `**/*.ext` style matching, basename matching for patterns like `*.ts`, type filters, modification-time sorting, and tool schema exposure. |
| 9.3 Grep-style content search | Done | `ocli/src/tools.js`, tests | Enhanced `grep_files` with regex support, glob/type filters, `content`, `files_with_matches`, and `count` output modes while preserving existing literal-query behavior. |
| 9.4 Targeted file reads | Done | `ocli/src/tools.js`, tests | `read_file` now supports `offset`, `limit`, `numbered`, and `maxChars` for cat-n style line-range reads; default full-content reads remain backward compatible. |
| 9.5 Local validation | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |

## Phase 10 Sub-Agent v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 10.1 AgentTool gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/**`, `ocli/src/agent.js`, `ocli/src/tools.js` | Full `ocli-test` AgentTool includes fork/background/worktree/remote/teammate modes. This slice intentionally migrates the core synchronous delegation behavior first while preserving the Oases Web-owned model/API contract. |
| 10.2 `agent_run` tool schema | Done | `ocli/src/constants.js`, `ocli/src/tools.js` | Added `agent_run` as a first-class OpenAI tool schema with `task`, `description`, `agentType`, `contextFiles`, and `maxTurns`. Direct `/tools/agent_run` calls are rejected because execution requires parent agent context. |
| 10.3 Bounded nested agent execution | Done | `ocli/src/agent.js` | Parent agents can delegate to a fresh sub-agent using the same `apiBaseUrl`/`model` from Oases Web. Nested sub-agents do not receive `agent_run`, preventing unbounded recursion. Sub-agent tool calls use the same workspace and approval callback. |
| 10.4 Sub-agent session events | Done | `ocli/src/agent.js`, tests | Parent session emits `subagent_start`, `subagent_event`, and `subagent_done`; `agent_run` returns the sub-agent final text, stopped reason, tool results, invoked skills, and file artifacts to the parent model as a normal tool result. |
| 10.5 Local validation | Done | `scripts/ocli-smoke.mjs`, package/build | Smoke starts real ocli, verifies top-level `agent_run` exposure, rejects direct HTTP execution, confirms nested agents cannot recursively see `agent_run`, and validates sub-agent read-file work through persisted session events. `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. |

## Phase 11 Background Sub-Agent v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 11.1 Background AgentTool slice | Done | `ocli/ocli-test/src/tools/AgentTool/**`, `ocli/src/agent.js` | Migrated the first background delegation behavior from the `ocli-test` AgentTool direction. This is scoped to the current parent agent run rather than a cross-session daemon; future work can persist detached background tasks. |
| 11.2 `runInBackground` launch | Done | `ocli/src/tools.js`, `ocli/src/agent.js` | `agent_run` now accepts `runInBackground: true`, starts the sub-agent without blocking the parent model turn, and returns `async_launched` with a `subagentId`. |
| 11.3 `agent_status` polling | Done | `ocli/src/constants.js`, `ocli/src/tools.js`, `ocli/src/agent.js` | Added `agent_status` for checking one background sub-agent or listing all current background sub-agents. Direct `/tools/agent_status` calls are rejected outside an agent session. |
| 11.4 Background events/results | Done | `ocli/src/agent.js` | Parent session emits background `subagent_start`, forwarded `subagent_event`, `subagent_done`, and `subagent_error`. `agent_status` returns completed sub-agent final text, tool results, artifacts, and metadata. |
| 11.5 Local validation | Done | `scripts/ocli-smoke.mjs`, package/build | Smoke starts real ocli, verifies `agent_status` exposure/rejection, launches a background sub-agent, polls with `agent_status`, and verifies completion/result/events through the real session API. `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. |

## Phase 12 Worktree Sub-Agent Isolation v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 12.1 `ocli-test` worktree gap slice | Done | `ocli/ocli-test/src/utils/worktree.ts`, `ocli/ocli-test/src/bridge/bridgeMain.ts`, `docs/ocli-migration-plan.md` | Full `ocli-test` supports reusable named worktrees, hooks, cleanup flows, tmux, and bridge spawn modes. This Oases slice intentionally implements only detached git worktree isolation for `agent_run`. |
| 12.2 `agent_run` isolation schema | Done | `ocli/src/tools.js` | Added `isolation: "workspace" | "worktree"` to the tool schema. Default remains `workspace` for backward compatibility. |
| 12.3 Detached worktree execution root | Done | `ocli/src/agent.js` | Top-level `agent_run(isolation:"worktree")` now requires a git repo with a HEAD commit, creates a detached temp worktree from `HEAD`, and runs the sub-agent with that worktree as its filesystem root. Nested sub-agents still cannot call `agent_run`. |
| 12.4 Worktree result metadata | Done | `ocli/src/agent.js` | Sub-agent events and results include `isolation`, `worktreePath`, `gitRoot`, `baseRef`, and `headCommit`. Completed worktree sub-agents also return `workspaceStatus` so the parent can inspect isolated changes. |
| 12.5 Isolation smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke starts a real ocli server, has the fake model call `agent_run(isolation:"worktree")`, writes a file in the sub-agent, verifies the file exists only in the returned worktree path, checks status metadata, and removes the temporary test worktree. |
| 12.6 Local validation | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, and `node --check` for changed JS/MJS files passed. Build still has the known CSS minify warning. |
| 12.7 Future hardening | Pending | `ocli/src/agent.js`, Web UI | Add user-facing worktree cleanup/keep controls, optional branch naming, background persisted worktree tasks, and explicit merge/apply flows before treating this as full `ocli-test` parity. |

## Phase 13 Worktree Lifecycle Tools v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 13.1 Lifecycle gap slice | Done | `ocli/ocli-test/src/utils/worktree.ts`, `ocli/ocli-test/src/components/WorktreeExitDialog.tsx`, `docs/ocli-migration-plan.md` | `ocli-test` exposes keep/remove decisions and cleanup flows around worktrees. This slice gives Oases agents a tool-level lifecycle surface before a Web UI is added. |
| 13.2 Worktree discovery and validation | Done | `ocli/src/constants.js`, `ocli/src/tools.js` | Added `worktree_list` and shared validation that resolves `/var` vs `/private/var` with `realpath` and only accepts paths returned by `git worktree list --porcelain`. The main workspace is explicitly rejected for management operations. |
| 13.3 Worktree diff inspection | Done | `ocli/src/tools.js` | Added `worktree_diff`, backed by existing `workspace_status`, so agents can inspect isolated changes and untracked previews by worktree path. |
| 13.4 Selective apply | Done | `ocli/src/tools.js` | Added `worktree_apply` to copy selected changed files from a linked worktree into the main git root. It rejects rename/copy entries in v1 and refuses to overwrite dirty same-path main workspace changes unless `force: true`. |
| 13.5 Guarded removal | Done | `ocli/src/tools.js` | Added `worktree_remove`, which refuses to remove dirty worktrees unless `force: true`, then unregisters the linked worktree with `git worktree remove`. Oases temp parent folders are cleaned best-effort after removal. |
| 13.6 Agent messaging and permissions | Done | `ocli/src/agent.js`, `ocli/src/tools.js` | Agent-visible tool result messages now distinguish worktree lifecycle calls. `worktree_apply` and `worktree_remove` require approval during agent runs because they can modify or discard local files. |
| 13.7 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates a real isolated worktree through `agent_run(isolation:"worktree")`, verifies list/diff, rejects a non-linked path, refuses dirty removal without force, applies a selected file to the main workspace, removes the worktree with force, and verifies it is unregistered. |
| 13.8 Local validation | Done | scripts/build | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, and changed-file `node --check` passed. Build still has the known CSS minify warning. |
| 13.9 Future hardening | Pending | `ocli/src/tools.js`, Web UI | Add Web cards/buttons for inspect/apply/remove, richer conflict previews, rename support, commit/branch workflows, and persisted worktree registry for cross-session cleanup. |

## Phase 14 Web Worktree Control Surface v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 14.1 Web gap slice | Done | `src/pages/Home.tsx`, `docs/ocli-migration-plan.md` | Phase 13 exposed worktree lifecycle tools in ocli, but Web project mode still only showed generic artifacts/tool results. This slice adds a first Web control surface for those isolated outputs. |
| 14.2 Tool/type awareness | Done | `src/pages/Home.tsx` | Extended project tool names and local session detail normalization for `worktree_list`, `worktree_diff`, `worktree_apply`, and `worktree_remove`. Tool result `data` is now preserved in Web session detail normalization so worktree metadata is not lost. |
| 14.3 Worktree summary extraction | Done | `src/pages/Home.tsx` | Added extraction of worktree path, git root, branch/head, changed file counts, summaries, apply counts, and removed state from `agent_run`, `agent_status`, and worktree lifecycle tool results. |
| 14.4 Web controls | Done | `src/pages/Home.tsx` | The project workspace session detail panel now renders a Worktrees section with compact cards and actions to inspect, apply, or remove a linked worktree. Apply/remove use confirmation prompts and call the local ocli tool endpoints. |
| 14.5 Prompt/tool messaging | Done | `src/pages/Home.tsx` | Project-mode tool guidance and tool success messages now include worktree lifecycle tools so model/tool traces read coherently. |
| 14.6 Local UI verification | Done | local dev server, Playwright | Started Vite locally, opened Oases Chat with Playwright, verified the base page and project mode render. The only browser console error was expected `127.0.0.1:8787` connection refused because ocli was not running during this UI check. |
| 14.7 Validation | Done | scripts/build | `pnpm build`, `pnpm test:ocli`, and `pnpm test:ocli-package` passed. Build still has the known CSS minify warning. Local Playwright render check also passed for the base page and project mode; the only browser console error was expected `127.0.0.1:8787` connection refused because ocli was not running during the UI check. |
| 14.8 Deployment | Done preview | Vercel | Preview deployment `dpl_9MW4J7cxVrA6LxhKQmSnsqFoeoae`, URL `https://oases-chat-4k66z5wx0-qingtengs-projects.vercel.app`, inspect `https://vercel.com/qingtengs-projects/oases-chat/9MW4J7cxVrA6LxhKQmSnsqFoeoae`. Production promotion remains pending explicit approval. |

## Phase 15 Workspace Custom Agents v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 15.1 AgentTool definition gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/loadAgentsDir.ts`, `docs/ocli-migration-plan.md` | Full `ocli-test` supports built-in/plugin/user agents with tools, permissions, MCP servers, hooks, memory, background, and isolation. This Oases slice intentionally starts with workspace-local Markdown definitions only. |
| 15.2 Workspace discovery/read tools | Done | `ocli/src/constants.js`, `ocli/src/tools.js` | Added `agent_list` and `agent_read` for `.oases/agents/**/*.md`. Agent frontmatter supports `name`, `description`, `agentType`, `maxTurns`, `background`, `isolation`, plus documented `tools`/`skills` arrays for future enforcement. Path reads are restricted to `.oases/agents`. |
| 15.3 `agent_run(agentName)` defaults | Done | `ocli/src/tools.js`, `ocli/src/agent.js` | `agent_run` now accepts `agentName` or `agent` and loads the matching workspace definition. Custom definitions can provide default `description`, `agentType`, `maxTurns`, `runInBackground`, and `isolation`; explicit call arguments still take precedence. |
| 15.4 Custom prompt injection and metadata | Done | `ocli/src/agent.js` | The custom agent Markdown body is injected into the sub-agent system prompt as dedicated instructions. Sub-agent start/done/error events, status records, and `agent_run` results include `agentName` and `customAgent` metadata for Web/session audit use. |
| 15.5 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/reviewer.md`, verifies `agent_list`/`agent_read`, rejects path escape, checks `agent_run` schema includes `agentName`, and proves the custom prompt marker reaches the sub-agent while nested sub-agents still cannot call `agent_run`. |
| 15.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 15.7 Future hardening | Pending | `ocli/src/agent.js`, `ocli/src/tools.js`, Web UI | Support richer YAML parsing, plugin/built-in agent sources, MCP servers, hooks, memory, model/effort overrides, and Web UI for selecting named agents. |

## Phase 16 Custom Agent Tool Scoping v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 16.1 AgentTool scoping gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/agentToolUtils.ts`, `ocli/ocli-test/src/tools/AgentTool/runAgent.ts`, `docs/ocli-migration-plan.md` | `ocli-test` resolves agent `tools` and `disallowedTools` against available tools, then scopes the sub-agent session. This Oases slice implements exact-name workspace custom-agent scoping without MCP or permission-rule patterns yet. |
| 16.2 Metadata parsing | Done | `ocli/src/tools.js` | `agent_list` and `agent_read` now expose `disallowedTools` alongside `tools`, both parsed from comma-separated Markdown frontmatter. |
| 16.3 Tool schema filtering | Done | `ocli/src/tools.js`, `ocli/src/agent.js` | `listOpenAiTools` accepts `allowedToolNames` and `disallowedToolNames`; custom sub-agents only receive the scoped tool schemas. `disallowedTools` wins over `tools`. |
| 16.4 Execution-time enforcement | Done | `ocli/src/agent.js` | Every parsed/native tool call is checked against the current sub-agent scope before approval or execution. If a model manually emits a forbidden `<tool>` block, ocli returns a failed tool result instead of executing it. |
| 16.5 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/reader.md` with `tools: read_file, write_file` and `disallowedTools: write_file`, verifies the nested model request lacks forbidden schemas, forces a manual `write_file` block, and proves the file is not created. |
| 16.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 16.7 Future hardening | Pending | `ocli/src/tools.js`, `ocli/src/agent.js` | Add wildcard/pattern permission rules, MCP tool scoping, richer YAML arrays, built-in/plugin agent sources, and Web UI surfacing for restricted-agent policies. |

## Phase 17 Custom Agent Skill Preloading v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 17.1 Agent skill preload gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/runAgent.ts`, `ocli/ocli-test/src/tools/AgentTool/loadAgentsDir.ts`, `docs/ocli-migration-plan.md` | `ocli-test` can preload skills declared in agent frontmatter. This Oases slice preloads workspace-local `.oases/skills` only, through the existing Web-owned model/API path. |
| 17.2 Skill loading helper | Done | `ocli/src/agent.js` | Added shared normalization/loading for `skill_read` results so explicit `skill_read` and custom-agent preloads produce the same skill shape. |
| 17.3 Pre-first-turn injection | Done | `ocli/src/agent.js` | `agent_run(agentName)` now reads `customAgent.skills`, loads matching workspace skills, and injects a `<skill_context>` message before the sub-agent's first model request. |
| 17.4 Audit metadata and events | Done | `ocli/src/agent.js` | Preloaded skills are recorded in `invokedSkills` and emit `skill_loaded` events with `preloaded: true`, which are forwarded through the parent session as `subagent_event`. |
| 17.5 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/skilled.md` with `skills: research`, verifies the sub-agent receives `Research Skill` and a preload marker before calling tools, and checks `invokedSkills` plus nested `skill_loaded` events. |
| 17.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 17.7 Future hardening | Pending | `ocli/src/tools.js`, `ocli/src/agent.js` | Add richer YAML array parsing, missing-skill warnings instead of hard failures where appropriate, built-in/plugin skill sources, and Web UI surfacing for preloaded skill metadata. |

## Phase 18 Custom Agent initialPrompt v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 18.1 Agent initialPrompt gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/loadAgentsDir.ts`, `ocli/src/tools.js`, `ocli/src/agent.js` | `ocli-test` supports `initialPrompt` on custom agent definitions and prepends it to the first user turn. This Oases slice implements the same workspace-local frontmatter behavior without migrating hooks, MCP servers, memory, or model overrides yet. |
| 18.2 Metadata parsing and discovery | Done | `ocli/src/tools.js` | `agent_list` and `agent_read` now expose trimmed `initialPrompt` frontmatter for `.oases/agents/**/*.md`. |
| 18.3 First-turn prompt prepend | Done | `ocli/src/agent.js` | `agent_run(agentName)` carries `customAgent.initialPrompt` into the normalized sub-agent request and prepends it before `子代理任务` in the sub-agent's first user message. |
| 18.4 Audit/result metadata | Done | `ocli/src/agent.js` | `agent_run` results, sub-agent records, and sub-agent events preserve `customAgent.initialPrompt` so Web/session audit can explain why the sub-agent had seeded first-turn instructions. |
| 18.5 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/starter.md` with `initialPrompt: initial prompt marker`, verifies discovery/read metadata, confirms the first sub-agent request contains the marker, and checks `agent_run` result metadata. |
| 18.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 18.7 Future hardening | Pending | `ocli/src/tools.js`, `ocli/src/agent.js` | Add YAML block scalar/list parsing parity, explicit Web surfacing for initial prompts, and later integrate initialPrompt with built-in/plugin/user agent source precedence once those sources are migrated. |

## Phase 19 Agent Frontmatter YAML Compatibility v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 19.1 Frontmatter compatibility gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/loadAgentsDir.ts`, `ocli/src/tools.js`, `scripts/ocli-smoke.mjs` | `ocli-test` custom agents are parsed as real frontmatter, so users naturally write YAML lists and block scalars. This Oases slice supports the common subset needed by workspace custom agents without adding a full YAML dependency. |
| 19.2 YAML list parsing | Done | `ocli/src/tools.js` | `tools`, `disallowedTools`, and `skills` now accept `- item` YAML lists in addition to comma-separated strings and simple inline arrays. |
| 19.3 Block scalar parsing | Done | `ocli/src/tools.js` | `initialPrompt: \|` and folded `initialPrompt: >` blocks are parsed, deindented, and passed through the existing first-turn prepend path. |
| 19.4 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/yamlstarter.md` with YAML list tools/disallowedTools/skills and a multi-line `initialPrompt`, verifies discovery/read metadata, tool scope filtering, skill preloading, and first-turn prompt injection. |
| 19.5 Docs | Done | `README.md`, `ocli/README.md`, `docs/ocli-migration-plan.md` | Documented comma-separated and YAML list syntax plus multi-line `initialPrompt` block syntax. |
| 19.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 19.7 Future hardening | Pending | `ocli/src/tools.js` | Replace the small frontmatter subset with a vetted YAML parser if future agent sources need nested objects for hooks/MCP servers, while preserving current package/runtime constraints. |

## Phase 20 Custom Agent Effort v1 Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 20.1 Agent effort gap slice | Done | `ocli/ocli-test/src/tools/AgentTool/loadAgentsDir.ts`, `ocli/src/tools.js`, `ocli/src/agent.js` | `ocli-test` custom agents can declare `effort`. This Oases slice supports named effort levels only and keeps model/API selection owned by Oases Web. |
| 20.2 Metadata parsing | Done | `ocli/src/tools.js` | `agent_list` and `agent_read` now expose valid `effort: low|medium|high|max` frontmatter values for workspace custom agents. |
| 20.3 Sub-agent request override | Done | `ocli/src/agent.js` | `agent_run(agentName)` now passes the custom agent effort into the nested `runAgent` call, which sets both `effort` and `reasoning_effort` on the Web proxy request. |
| 20.4 Audit/result metadata | Done | `ocli/src/agent.js` | Sub-agent events, records, background launch payloads, and `agent_run` results include the effective custom-agent effort when present. |
| 20.5 Smoke coverage | Done | `scripts/ocli-smoke.mjs` | Smoke creates `.oases/agents/effortful.md` with `effort: low`, verifies discovery/read metadata, checks the nested fake API receives `effort=low` and `reasoning_effort=low`, and confirms result metadata. |
| 20.6 Local validation | Done | scripts/build | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. |
| 20.7 Future hardening | Pending | `ocli/src/tools.js`, `ocli/src/agent.js` | Decide whether numeric effort should ever be accepted for Oases, add UI surfacing for per-agent effort, and later evaluate model override semantics without breaking Web-owned model selection. |

## Phase 21 Standalone CLI Push Folder Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 21.1 Create clean standalone package folder | Done | `oases-ocli/**` | Added a root-level CLI-only folder for future separate GitHub/npm publishing. It mirrors the active runtime without `ocli-test`, `legacy-mvp-src`, Web frontend files, Vercel files, generated artifacts, tarballs, or local `.oases` state. |
| 21.2 Package manifest and entrypoints | Done | `oases-ocli/package.json`, `oases-ocli/bin/ocli.js`, `oases-ocli/index.js` | Standalone package keeps the `oases-ocli`/`ocli` bin entries and adds local `npm test`, `npm run test:smoke`, and `npm run test:package` scripts. |
| 21.3 Standalone smoke tests | Done | `oases-ocli/scripts/ocli-smoke.mjs`, `oases-ocli/scripts/ocli-package-smoke.mjs` | Test imports and startup paths now resolve inside `oases-ocli/`, so the folder remains testable after being pushed outside the Web monorepo. |
| 21.4 Ignore and publish boundaries | Done | `oases-ocli/.gitignore`, `oases-ocli/.npmignore`, `oases-ocli/package.json` | Git ignores local/generated files; npm package contents remain limited by `files` to `bin`, `src`, and `README.md`. |
| 21.5 Documentation sync | Done | `README.md`, `ocli/README.md`, `oases-ocli/README.md`, `docs/ocli-npm-release.md`, `docs/ocli-migration-plan.md` | README now explains what to push for a standalone CLI repo versus a full OasesChat repo. Release docs now point to `oases-ocli/` as the clean package source. |
| 21.6 Local validation | Done | scripts/build | `cd oases-ocli && npm test`, root `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify and chunk-size warnings. |

## Phase 22 Zero-Argument CLI Startup Checklist

| Item | Status | Files | Notes |
| --- | --- | --- | --- |
| 22.1 Default command behavior | Done | `ocli/src/cli.js`, `oases-ocli/src/cli.js` | `ocli` now defaults to `serve` with `workspace = process.cwd()`. `ocli --workspace <path>` and `ocli -p <port>` also start directly. `ocli --help` still prints help. |
| 22.2 Backward compatibility | Done | `ocli/src/cli.js`, `oases-ocli/src/cli.js` | Existing `ocli serve --workspace <path>` and npm/pnpm `--` delimiter forms still parse as before. |
| 22.3 Smoke coverage | Done | `scripts/ocli-package-smoke.mjs`, `oases-ocli/scripts/ocli-package-smoke.mjs` | Package smoke now asserts zero-argument startup parsing, leading-flag parsing, and help text with the short `ocli` command. |
| 22.4 Documentation sync | Done | `README.md`, `ocli/README.md`, `oases-ocli/README.md`, `docs/ocli-npm-release.md`, `docs/ocli-migration-plan.md` | Docs now present the preferred published flow as `npm install -g oases-ocli` once, then `ocli` from the project directory. `npx oases-ocli` remains documented as one-off usage. |
| 22.5 Local validation | Done | scripts/build | `cd oases-ocli && npm test`, root `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify and chunk-size warnings. |

## Migration Rules

- Preserve the working Web protocol while migrating internals.
- Do not require local API keys; model/API must continue to come from Oases Web.
- Keep local filesystem access scoped to `--workspace`.
- Every phase must add or update smoke coverage before implementation changes where practical.
- After each meaningful ocli chunk, run `pnpm test:ocli-package` and `pnpm test:ocli`.
- After each large step, run `pnpm build` and deploy to Vercel.
- Keep `ocli/ocli-test/src` available as the reference source until its useful modules have been adapted.
- Keep `oases-ocli/` synchronized with active runtime changes before any standalone CLI GitHub push or npm release.

## Operation Log

| Time | Update | Validation | Deployment |
| --- | --- | --- | --- |
| 2026-06-10 15:45 | Created live migration plan. Phase 1 started. | Pending | Pending |
| 2026-06-10 17:47 | Phase 1 package structure completed: active runtime moved to `ocli/src`, package root created at `ocli`, old MVP archived under `ocli/legacy-mvp-src`, root entry and package smoke updated. | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed. | Pending |
| 2026-06-10 21:40 | Phase 1 deployed to production. Phase 2 started. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_FGnWbaraaR3pEoJ7BJQufMLD7S3u`, inspect `https://vercel.com/qingtengs-projects/oases-chat/FGnWbaraaR3pEoJ7BJQufMLD7S3u` |
| 2026-06-10 21:44 | Phase 2.1 and 2.2 completed: standardized tool result artifact metadata and preserved artifacts through agent results and Web normalization. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 2 first. |
| 2026-06-10 21:47 | Phase 2.3 completed: added `todo_write` structured project checklist tool and Web preview support. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 2 first. |
| 2026-06-10 21:49 | Phase 2.4 completed: enhanced `fetch_url` with HTML title and normalized link metadata for crawler/page-analysis workflows. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 2 first. |
| 2026-06-10 21:53 | Phase 2.5-2.7 completed: command/Python generated-file artifacts, final artifact-aware prompt guidance, and full validation. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending |
| 2026-06-10 21:54 | Phase 2 deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_Gx1DFBtJ6w5V5PdnguKmhq4ETLYQ`, inspect `https://vercel.com/qingtengs-projects/oases-chat/Gx1DFBtJ6w5V5PdnguKmhq4ETLYQ` |
| 2026-06-10 21:58 | Phase 3.1 completed: read-only shell command policy added so harmless commands like `pwd` no longer require approval, while Python/arbitrary shell/destructive tools remain gated. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 3 first. |
| 2026-06-10 22:00 | Phase 3.2 completed: approval requests now carry category and reason, and Web approval cards display them. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 3 first. |
| 2026-06-10 22:05 | Phase 3.3 completed: agent approval requests now include stable approval keys; sessions cache approved keys and reuse identical approvals within the same session. Smoke test also covers repeated Python execution with only one `approval_required` event. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Not deployed yet; continue Phase 3.4 first. |
| 2026-06-10 22:08 | Phase 3.4-3.5 completed: stronger destructive command detection added, read-only shell policy no longer auto-approves chained commands, and full local validation passed. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending Phase 3 production deploy. |
| 2026-06-10 22:10 | Phase 3 deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_9yWQmFGKqbsxnKzntLpo3jJe29SN`, inspect `https://vercel.com/qingtengs-projects/oases-chat/9yWQmFGKqbsxnKzntLpo3jJe29SN` |
| 2026-06-10 22:11 | Phase 4 started: detailed checklist added for continuation contract, auto-continue slices, completion criteria, long-running task smoke, and crawler-style smoke. | Pending | Pending |
| 2026-06-10 22:17 | Phase 4.1-4.6 completed: local agent now auto-continues across bounded slices, follows up on unfinished no-tool replies, Web shows auto-running state, and crawler-style artifact workflow is covered by smoke. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending Phase 4 production deploy. |
| 2026-06-10 22:19 | Phase 4 deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_ALXijhHX9UTUkCB7GZU4BTxcf65P`, inspect `https://vercel.com/qingtengs-projects/oases-chat/ALXijhHX9UTUkCB7GZU4BTxcf65P` |
| 2026-06-10 22:24 | Phase 5 started: detailed checklist added for session audit summaries, upgraded Web session detail panel, persisted restart recovery smoke, and manual resume affordance. | Pending | Pending |
| 2026-06-10 22:28 | Phase 5.1-5.5 completed: session details now expose artifacts/todos/approval summary/resume prompt, Web project workspace renders those audit fields, and restart recovery smoke covers persisted crawler artifacts. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending Phase 5 production deploy. |
| 2026-06-10 22:29 | Phase 5 deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_BgPsPDizHReArAF4u1SX8NLkFLgm`, inspect `https://vercel.com/qingtengs-projects/oases-chat/BgPsPDizHReArAF4u1SX8NLkFLgm` |
| 2026-06-10 22:30 | Phase 6 started: first slice scoped to workspace-local skill discovery/read tools before full forked skill invocation. | Pending | Pending |
| 2026-06-10 22:35 | Phase 6.1-6.3 and 6.5 completed: added workspace-local `skill_list`/`skill_read`, prompt guidance, and smoke coverage for skill discovery/read/path boundaries. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending Phase 6 first-slice production deploy. |
| 2026-06-10 22:36 | Phase 6 first slice deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_9xAowqPdNExrhFQoYmgixGdmQaBY`, inspect `https://vercel.com/qingtengs-projects/oases-chat/9xAowqPdNExrhFQoYmgixGdmQaBY` |
| 2026-06-10 22:45 | Phase 6.4 skill invocation v1 completed: `skill_read` now loads skills into current-session `<skill_context>`, emits `skill_loaded`, records `invokedSkills`, and smoke verifies skill-guided output generation. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed. | Pending Phase 6 skill invocation v1 deploy. |
| 2026-06-10 22:46 | Phase 6 skill invocation v1 deployed to production. | Vercel build passed. | `https://www.oasesai.xyz`, deployment `dpl_EQSzr6N87tvan1MJYxsKWsyJYNMu`, inspect `https://vercel.com/qingtengs-projects/oases-chat/EQSzr6N87tvan1MJYxsKWsyJYNMu` |
| 2026-06-10 22:49 | Phase 7 started: strengthened npm package smoke with real tarball install, added public publish config, and updated npm install/release docs. Repository metadata, license, workflow, and real publish remain blocked pending user decisions. | Pending validation | Not applicable |
| 2026-06-10 22:50 | Phase 7.1-7.3 validated: package smoke now covers `npm pack --dry-run`, real tarball creation, clean temp-project install, and installed `oases-ocli --help`; ocli smoke and Web build also passed. | `pnpm test:ocli-package`, `pnpm test:ocli`, `pnpm build` passed. | Not deployed; package/docs change does not affect Vercel runtime. |
| 2026-06-10 22:51 | Phase 7.6 validation workflow added: GitHub Actions now runs package smoke, ocli smoke, and Web build on relevant PR/push changes. | Workflow file added; local validation already passed at 22:50. | Not deployed; CI workflow change does not affect Vercel runtime. |
| 2026-06-10 23:10 | Phase 8.1-8.3 completed: added interactive green Oases terminal animation and startup guidance while preserving plain output for CI/scripts. | Pending validation | Not deployed; local ocli terminal UX change only. |
| 2026-06-10 23:16 | Phase 8.4 validated: ocli terminal UX passed local smoke/package/build checks; SIGINT now exits cleanly so `pnpm ocli ...` does not treat a user Ctrl+C stop as a lifecycle failure. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli terminal UX change only. |
| 2026-06-10 23:58 | Phase 8.5 completed: terminal icon redesigned from a sampled Oases PNG silhouette instead of a generic mathematical blob, improving likeness to the provided icon while keeping the flowing green status effect. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli terminal UX change only. |
| 2026-06-11 00:08 | Phase 8.6-8.7 completed: terminal animation now makes the two Oases lobes visibly exchange size, and interactive startup schedules Oases Web to open automatically after 6 seconds. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli terminal UX change only. |
| 2026-06-11 00:35 | Phase 9 completed: added `ocli-test`-style search/read improvements with `glob_files`, regex/type/output-mode `grep_files`, and targeted numbered `read_file` ranges. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 00:50 | Phase 10 completed: added synchronous `agent_run` sub-agent delegation v1, bounded nested execution, sub-agent event forwarding, and ocli smoke coverage through the real local server/session API. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 01:06 | Phase 11 completed: added current-session background sub-agents via `agent_run(runInBackground: true)` plus `agent_status` polling, with real ocli smoke coverage. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build` passed with known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 01:36 | Phase 12 completed: added `agent_run(isolation:"worktree")`, detached git worktree execution for sub-agents, worktree metadata/status return, and smoke coverage proving isolated writes do not pollute the main workspace. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, and `node --check` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 10:37 | Phase 13 completed: added worktree lifecycle tools (`worktree_list`, `worktree_diff`, `worktree_apply`, `worktree_remove`) with linked-worktree path validation, guarded apply/remove behavior, realpath handling for macOS `/var` vs `/private/var`, and smoke coverage through real ocli HTTP/tool/session paths. | `pnpm test:ocli`, `pnpm test:ocli-package`, `pnpm build`, and `node --check` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 11:01 | Phase 14 completed: Web project workspace session detail now extracts worktree metadata from ocli session results and renders inspect/apply/remove controls for isolated sub-agent worktrees. | `pnpm build`, `pnpm test:ocli`, `pnpm test:ocli-package`, and local Playwright render check passed. Build still has the known CSS minify warning; Playwright saw expected ocli connection refused while ocli was not running. | Preview deployed: `https://oases-chat-4k66z5wx0-qingtengs-projects.vercel.app`, deployment `dpl_9MW4J7cxVrA6LxhKQmSnsqFoeoae`. Production promotion pending approval. |
| 2026-06-11 11:29 | Phase 15 completed: added workspace custom agent discovery/read tools and `agent_run(agentName)` prompt injection with custom agent metadata in sub-agent events/results. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 11:52 | Phase 16 completed: custom workspace agents now scope nested tool schemas with `tools`/`disallowedTools` and block forbidden manual tool calls before execution. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 12:11 | Phase 17 completed: custom workspace agents now preload declared workspace skills before the first sub-agent turn and record preloaded skills in audit events/results. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 13:24 | Phase 18 completed: custom workspace agents now parse `initialPrompt`, prepend it to the sub-agent first user turn, and preserve it in custom-agent result metadata. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 13:30 | Phase 19 completed: custom agent frontmatter now supports YAML list fields, inline arrays, and multi-line `initialPrompt` block scalars for closer `ocli-test` compatibility. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 19:18 | Phase 21 completed: created `oases-ocli/` as a clean standalone CLI push folder, added standalone tests and ignore files, and updated README/release docs with push boundaries. | `cd oases-ocli && npm test`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify and chunk-size warnings. | Not deployed; docs/package structure change only. |
| 2026-06-11 16:37 | Phase 20 completed: custom workspace agents now parse `effort`, apply it to sub-agent model requests, and preserve it in events/results while keeping Web-owned model/API unchanged. | `node --check`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify warning. | Not deployed; local ocli runtime/tooling change only. |
| 2026-06-11 19:28 | Phase 22 completed: changed CLI parsing so `ocli` starts the runtime directly, kept `serve` compatibility, and updated package smoke/docs for the short command. | `cd oases-ocli && npm test`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `pnpm build` passed. Build still has the known CSS minify and chunk-size warnings. | Not deployed; local CLI UX/docs change only. |
| 2026-06-11 19:41 | Phase 7.4 completed after standalone GitHub publication: recorded `https://github.com/qingtengCHINA/oases-ocli` in package metadata and docs, added standalone CLI GitHub Actions CI. | `cd oases-ocli && npm test`, standalone `npm run test:package`, and root `pnpm test:ocli-package` passed. | Not deployed; package metadata/docs/CI change only. |
| 2026-06-11 19:49 | Phase 7.5 completed: set oases-ocli license to Apache-2.0, added LICENSE files in both standalone and integrated CLI package folders, and updated package smoke/docs. | `cd oases-ocli && npm test`, standalone `npm run test:package`, and root `pnpm test:ocli-package` passed. | Not deployed; package metadata/docs/license change only. |
| 2026-06-11 20:45 | Phase 23 completed and promoted via production deploy: polished Web project-mode UX with desktop ocli install guidance when disconnected, mobile-only desktop-use hinting, smaller text-scale model waiting loader, tighter composer fade, and green WebGL sidebar branding with rotating mode word. | `pnpm build` passed locally with known CSS minify/chunk warnings; Vercel production build completed. Local Browser validation passed for desktop disconnected guide, mobile no-install-command hint, connected ocli guide hiding, sidebar WebGL/rotating brand, and clean console. | Production deployed: `https://oases-chat-a7gnvu9n7-qingtengs-projects.vercel.app`; aliased to `https://www.oasesai.xyz`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/HqFpHmnB4Rf2n4gPHe18KWEVDRui`. |
| 2026-06-11 21:39 | Phase 7.7 completed: published `oases-ocli@0.1.0` to the npm registry and restored Web/docs install guidance to `npm install -g oases-ocli`. | `cd oases-ocli && npm test`, `pnpm test:ocli-package`, `pnpm build`, `npm view oases-ocli`, and clean temp-prefix `npm install -g oases-ocli && ocli --help` passed. Build still has known CSS minify/chunk warnings. | npm package published; Web production already shows the registry install command. |
| 2026-06-11 22:10 | Phase 24 completed: workspace-internal absolute paths are accepted and normalized, Web project prompt now carries oases identity/path rules, and frontend pre-validation no longer blocks connected ocli absolute paths. | `pnpm test:ocli`, `pnpm test:ocli-package`, `cd oases-ocli && npm test`, `pnpm build`, `npm view oases-ocli`, and local `npm install -g oases-ocli@latest && ocli --help` passed. | Published `oases-ocli@0.1.1`; production deployed: `https://oases-chat-km60bnpyk-qingtengs-projects.vercel.app`; aliased to `https://www.oasesai.xyz`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/AndJZaXQsPF7hXtWqhk78stQTP6w`. |
| 2026-06-11 22:46 | Phase 25 started: added the documented `ocli update` self-update command that runs `npm install -g oases-ocli@latest`. | `node --check`, `pnpm test:ocli-package`, and `cd oases-ocli && npm test` passed. `npm publish --access public` prepared `oases-ocli@0.1.2` tarball but npm returned EOTP. | Waiting for npm 2FA OTP to publish `0.1.2`. |
| 2026-06-11 23:36 | Web sidebar brand visual fix: kept the WebGL liquid-metal Oases icon, removed sidebar-only mask/blend/shadow styling that made the canvas bounds read as a black square, and enabled transparent alpha cropping for the small sidebar mark. | `pnpm build` passed with known CSS minify/chunk warnings; local Playwright screenshot verified the sidebar brand no longer shows a distinct black icon background block. | Preview deployed: `https://oases-chat-cqoq3e9mm-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/2MQPm99aUNuAouzLwPGFwx3bMdNg`. |
| 2026-06-11 23:43 | Promoted the sidebar WebGL icon transparency fix to production. | Vercel production build passed with known chunk-size warning. | Production deployed: `https://oases-chat-7de1ca84u-qingtengs-projects.vercel.app`; aliased to `https://www.oasesai.xyz`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/7DJ9t8gpPjYjitL9rPK4nVRi6iZh`. |
| 2026-06-12 00:06 | Phase 26 completed: fixed Windows legacy cmd refresh bug by disabling animated full-screen redraw outside explicit ANSI-capable Windows terminals, while keeping Windows Terminal/VS Code animation and adding `OCLI_ANIMATED_UI=1` force mode. | `node --check ocli/src/terminalUi.js`, `node --check oases-ocli/src/terminalUi.js`, `pnpm test:ocli-package`, `pnpm test:ocli`, `cd oases-ocli && npm test`, `npm view oases-ocli@latest version dist.tarball --json`, and `npm install -g oases-ocli@latest && ocli --help` passed. | Published `oases-ocli@0.1.3`; local global install now reports `ocli 0.1.3`. |
| 2026-06-12 00:34 | Phase 27 completed: upgraded Web project-mode onboarding with OS-specific ocli setup tabs, clickable copy commands, npm-latest version detection after connection, and stale-version update guidance. | `pnpm build` passed with known CSS/chunk warnings; Playwright verified desktop disconnected setup guide, mocked stale `0.1.1` to latest `0.1.3` update guide, command clipboard copy, and mobile no-install-command behavior. | Preview deployed: `https://oases-chat-9p4oi61rv-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/2QG9tKo4iuyTLk536iQBTFEZbh34`. |
| 2026-06-12 00:41 | Phase 27 refinement: update guide now shows exactly one command. Versions older than `0.1.2` get `npm install -g oases-ocli@latest`; `0.1.2+` gets `ocli update`. | `pnpm build` passed; Playwright mocked `0.1.1` and `0.1.2` connected runtimes and verified exactly one update command in both cases. | Preview deployed: `https://oases-chat-8mhhr3xtj-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/FGeq4n7XDSTFXpf693eDu3iuuqs3`. |
| 2026-06-12 01:18 | Phase 28 completed: added Node.js check/install commands to project-mode onboarding, bounded project workspace modal overflow with internal scrolling, removed dubious public model labels in favor of Oases capability labels, fixed absolute OG/Twitter images, hid empty default Recents, added a clickable WebGL Oases intro page, and reduced ocli approval noise so read commands run by default while destructive tools and file-modifying shell/Python commands still request approval. | `node --check ocli/src/tools.js && node --check oases-ocli/src/tools.js`, `pnpm build`, `pnpm test:ocli-package`, `pnpm test:ocli`, `cd oases-ocli && npm test`, and Playwright QA passed. Build still has known CSS/chunk warnings. | Preview deployed: `https://oases-chat-9uewdlktg-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/5n16F4Ut1VoSHgenDCCYC95kRVsS`. |
| 2026-06-12 10:48 | Phase 28 layout/icon follow-up: moved the disconnected ocli setup guide out of the fixed composer into the main scroll area with its own bounded scroll, and changed both the sidebar brand icon and empty-state welcome icon into native `#/about` links so browsers reliably open the Oases intro page. | `pnpm build` passed; Playwright verified project setup stays above the composer in a 1440x900 viewport and welcome icon is an `<a href="#/about" target="_blank">` that opens the intro page. | Preview deployed: `https://oases-chat-9eyd3e85t-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/Hck6EDMnTZNQ4fWLnoFnMxTyFjaU`. |
| 2026-06-12 11:04 | Promoted the Phase 28 follow-up build to production after user approval. This production version includes bounded project-mode ocli onboarding, reliable Oases intro-page links for both WebGL icons, beginner setup guidance, stale-ocli version guidance, and reduced approval noise. | Vercel production inspection reports deployment `dpl_GRLbggcXfNo7Ys49Qc84DErJ5vUz` as `READY` with target `production`; `npm view oases-ocli version dist-tags --json` reports latest `0.1.4`. Vercel build passed with known Vite chunk-size and pnpm/esbuild warnings only. | Production deployed: `https://www.oasesai.xyz`; deployment URL: `https://oases-chat-dejzq2y29-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/GRLbggcXfNo7Ys49Qc84DErJ5vUz`. |
| 2026-06-12 12:22 | Phase 29 review hardening completed: ocli streamed SSE responses incrementally instead of waiting for full response text; `fetch_url` now manually validates each redirect and validates actual socket DNS results while bounding response reads; agent shell/Python execution now requests approval by default; workspace path checks resolve symlinks with `realpath`; Web conversation persistence is debounced and streaming scroll uses sticky-bottom auto-scroll. `oases-ocli` package version prepared as `0.1.5`. | `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Added smoke coverage for private-host `fetch_url` rejection and symlink workspace escape rejection. Build still has known CSS/chunk warnings. | Not deployed yet; npm latest remains `0.1.4` until `oases-ocli@0.1.5` is published. |
| 2026-06-12 12:41 | Phase 29 patch was published by the user; `npm view oases-ocli version dist-tags --json` now reports latest `0.1.5`. Phase 30 completed: default local token auth added so loopback tools/agents cannot be driven without the terminal token; Web can import the token from the auto-open hash link or via the project-mode token field. Local package/runtime version prepared as `0.1.6`. | `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Local HTTP smoke: `/health` without token returns 401 + `authRequired`, `/health` and `/tools` with `test-token` succeed, `/tools` without token returns 401. | Preview deployed: `https://oases-chat-4ji7ca2yp-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/HdPhHpUcdrNuKM9HVxs3aA9ofmUR`; Vercel reports `READY` target `preview`. Direct curl returns 401 because Preview protection is enabled. npm latest remains `0.1.5` until `oases-ocli@0.1.6` is published. |
| 2026-06-12 13:25 | Promoted Phase 30 Web token-auth UI to production after user approval. Production now includes token hash import, project-mode token entry/save/clear, and Authorization headers for local ocli requests. | Vercel production build passed with known Vite chunk-size/pnpm warnings. Vercel inspect reports deployment `dpl_9K6EfDTmrbDCdBrzh3A4xtRXgz2i` as `READY` target `production`. HTTP smoke: `https://www.oasesai.xyz/` returns 200 and `https://www.oasesai.xyz/#/?ocliToken=test-token` returns 200. | Production deployed: `https://www.oasesai.xyz`; deployment URL: `https://oases-chat-qk0p2sya3-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/9K6EfDTmrbDCdBrzh3A4xtRXgz2i`. npm latest remains `0.1.5` until `oases-ocli@0.1.6` is published. |
| 2026-06-12 13:54 | Phase 31 completed: added `ocli open` and `ocli open --dry-run` so a running workspace can reopen the tokenized Oases Web URL from `.oases/ocli/runtime.json` without manual token copying. Root and standalone CLI docs now include the recovery command. | `node --check ocli/src/open.js`, `node --check oases-ocli/src/open.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. The first parallel run hit a temporary `8798` port collision; serial rerun passed. | npm registry now reports `oases-ocli@0.1.6` as `latest`. |
| 2026-06-12 14:10 | Phase 32 completed: moved Web conversation persistence to IndexedDB with localStorage migration, lazy-loaded Markdown rendering, added Vitest unit coverage for project-message parsing and semver comparison, updated e2e storage fixture handling for IndexedDB, and archived root debug/OilPrice artifacts under `docs/archive/root-artifacts/`. | `pnpm test:unit`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, `pnpm build`, and focused Playwright `switching models inside an existing conversation` desktop test passed. Build still has known CSS minify and chunk warnings. | Preview deployed: `https://oases-chat-ojbcv2fe1-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/39kKq5nAPsw3iXEzf8GH5WyTDXaY`; Vercel reports `READY`; direct curl returns 401 because Preview Protection is enabled. |
| 2026-06-13 00:00 | Phase 33 completed: standardized public model names and IDs across text, image, audio, and project modes. Text now defaults to Oases Chat (`mimo-v2.5`); image defaults to Oases Image (`grok-imagine-image`); audio uses Oases Voice (`mimo-v2.5-tts`); project defaults to Oases Code (`deepseek-v4-pro`) and offers Oases Project (`mimo-v2.5-pro`). Oases Ultra (`glm-5.1`) includes a local daily 5-use guard. Removed old user-facing MiMo Omni image guidance and updated e2e expectations for the new model names. | `pnpm test:unit`, focused Playwright model spec suite, `pnpm build`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Build still has known CSS minify and chunk warnings. | Preview deployed: `https://oases-chat-8oaq636z7-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/HyYc6wgykcCixkNquGaQA7Smprrc`; Vercel reports `READY`; direct curl returns 401 because Preview Protection is enabled. |
| 2026-06-13 11:18 | Promoted Phase 33 public model spec to production after user approval. | Vercel production build passed with known chunk-size warning and pnpm/esbuild script warning only. HTTP smoke returned 200 for `https://www.oasesai.xyz/` and `https://www.oasesai.xyz/#/?ocliToken=test-token`. | Production deployed: `https://www.oasesai.xyz`; deployment URL: `https://oases-chat-5kcvxxc5r-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/54uChCfccv5abukeM6RjasQ2Hq9M`. |
| 2026-06-15 20:20 | Phase 34 completed: fixed welcome greeting clipping, lowered image-generation animation beneath status text, fixed the `document.startViewTransition` illegal invocation that blocked light-mode switching, polished theme-variable shadows/backgrounds for light mode, rebuilt the Oases intro page with multilingual copy and real footer links, and added a new Terms of Service route. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings; Browser QA passed for desktop greeting, light-mode toggle, About/Terms routes, footer Features/Models scrolling, loader z-index CSS, and mobile no-overflow checks. | Preview deployed: `https://oases-chat-qcmzxwl1z-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/Gw7Z5caRvbApSBa4qffnnevaETG2`. |
| 2026-06-15 20:54 | Phase 35 completed: added a document disguise mode from the chat header. The mode renders a generic dark document editor surface with titlebar, ribbon tabs, style gallery, centered page, inline chat composer, existing message rendering, generated media support, status bar, and one-click exit. Mobile layout hides nonessential status items and keeps the ribbon horizontally scrollable without page-level overflow. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings; Browser QA passed for desktop entry, input enablement, exit flow, no console errors, and 390x844 mobile no-overflow behavior. | Preview deployed: `https://oases-chat-pwf5al9ul-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/LbDNbm6n4o32xYzUMk82QJ3XrnrR`. |
| 2026-06-15 21:20 | Phase 35 follow-up completed: document disguise mode no longer reuses the normal chat feed auto-scroll ref, so model output does not force the document to the bottom. Role bars and the input bar are now static markers with no caret blink. The bottom-right zoom now has real `- / slider / +` controls that resize the document page width and page text from 70% to 170%; default zoom is 100% to avoid unnecessary horizontal scroll. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Browser QA passed for default 100% desktop no-overflow, 130% real zoom, submit-without-bottom-jump behavior, static marker animation, and 390x844 mobile no-overflow/statusbar checks. | Preview deployed: `https://oases-chat-ifwq5hw1m-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/9GkrvChWjZNfgQZKMVVjaUrC1sUy`. |
| 2026-06-15 21:31 | Phase 35 empty-document layout follow-up completed: new-conversation document disguise now gives the welcome greeting its own lower body position and adds extra blank-document spacing before the inline composer, so the greeting no longer overlaps the crop marks or feels pinned to the top. Mobile gets a smaller spacing scale to preserve usable page height. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Browser QA passed for desktop empty document spacing, mobile empty document spacing, no page overflow, and clean console. | Preview deployed: `https://oases-chat-l4j1wixlm-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/3cc8bLHD9MPKeMe3DiiGjWJeyihC`. |
| 2026-06-15 21:44 | Promoted Phase 35 document disguise fixes to production after user approval. Production now includes the Word-like document disguise mode, no document-mode auto-bottom scroll, static role markers, real document zoom, and empty-document greeting spacing fixes. | Vercel production build passed with known Vite chunk-size and pnpm/esbuild script warnings only. Deployment reports `READY` with target `production` and alias `https://www.oasesai.xyz`. | Production deployed: `https://www.oasesai.xyz`; deployment URL: `https://oases-chat-puqhah1bs-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/AZxWxx3BoQQK1WMJvEpynGMD58mD`. |
| 2026-06-15 22:13 | Phase 36 completed: document disguise is now a real editable document surface. The page initializes a per-conversation document draft from the current chat, lets users edit directly in the document body, runs real formatting commands through the ribbon, appends new chat messages into the draft, and exports the current document as Word-compatible `.doc` or `.html`. Mobile no longer hides export tools; they remain reachable through the horizontal ribbon. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright desktop check passed for editable body, heading/bold formatting, and `.doc` download. Playwright mobile check passed for editable body, `.html` download, no page/status overflow, and scrollable ribbon. | Preview deployed: `https://oases-chat-7it7zf1ji-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/8UjLGXCY9HuP96HLrjQxNgp5Zw8m`. |
| 2026-06-15 23:25 | Phase 37 completed: corrected document mode direction so AI remains inside the Word document body, but no longer appears as raw chat. User turns now render as Word-like review notes/edit instructions, assistant turns render as Oases revision blocks, active assistant revision content updates during streaming, and the Ribbon now has real file/insert/layout/review/view commands plus ruler/page-layout/print polish. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Browser desktop QA passed for existing conversation rendering without `You`/`Assistant` labels. Playwright desktop verified insert table, no footer clipping, no page overflow; Playwright mobile verified no page overflow, scrollable Ribbon, no footer clipping. | Preview deployed: `https://oases-chat-1xumb1g7d-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/7NavDtRpb7uUtKyG1D6JafLrmPpP`. |
| 2026-06-16 00:12 | Phase 38 completed: user rejected visible AI/product labels in document mode. The document disguise now behaves like a normal Word editor surface: no `Oases 编辑指令`, no visible AI labels, no instruction cards; user text appears as a normal paragraph, pressing Enter submits it, and the generated reply appears as the next paragraph. Empty documents no longer insert a body title. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright desktop mocked a streamed reply and verified no forbidden text, one user paragraph, one assistant paragraph, cleared input, no footer clipping, and no overflow. Playwright mobile verified no forbidden text, no overflow, scrollable Ribbon, and no footer clipping. | Preview deployed: `https://oases-chat-2m6ilk403-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/59hvojGX129p3ZC8vrffFM52FJtg`. |
| 2026-06-16 00:28 | Phase 39 completed: restored the original model waiting animation inside the full Word paragraph disguise. Empty assistant paragraphs now show a 4x4 MatrixLoader-style point animation with no visible text label, and the loader is replaced by the generated paragraph as streaming content arrives. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright delayed-stream check verified 16 loader cells, no visible loader label, no forbidden product/instruction text, and automatic replacement with final paragraph text. | Preview deployed: `https://oases-chat-5j8ikm77h-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/4C3LKB92dMX39A4LNy3RqvfxtpuT`. |
| 2026-06-16 12:25 | Phase 40 completed: continued Word editor fidelity by replacing the remaining document-mode textarea composer with a true `DIV contentEditable` current paragraph. The hidden submit button stays visually absent, Enter submits the paragraph, Shift+Enter remains available for multiline draft text, the draft line clears after generation, focus returns to the current paragraph, and `Cmd/Ctrl+S`, `Cmd/Ctrl+P`, `Cmd/Ctrl +/-/0` now behave like editor shortcuts for save, print, and zoom. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright desktop check verified `.word-inline-input` is a contentEditable `DIV`, there are zero textarea nodes inside the Word composer, hidden submit is still 1px/opacity 0, delayed-stream loader shows 16 cells with no visible label, generated text replaces the loader, forbidden AI/product/instruction labels remain absent, `Control+=` updates zoom to 110%, and no horizontal overflow occurs. Playwright mobile check verified the contentEditable input, zero textarea nodes, visible shell, and no horizontal overflow at 390x844. | Preview deployed: `https://oases-chat-n9it8fo7s-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/BthJb1Gph8RmCP9bqJQeWSHQi8CT`. |
| 2026-06-16 12:34 | Phase 41 completed: Word document drafts are now persisted independently from chat history. Manual edits inside `.word-editor-surface` are saved into IndexedDB key `document-drafts:v1` with the conversation id, HTML, synced message count, and timestamp; reopening document mode restores the saved HTML before appending new chat turns. Internal `data-placeholder` markers are stripped before persistence and export. Conversation deletion and clear-history now also clean matching document drafts. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright refresh-restore check verified one IndexedDB draft record, one saved conversation, active conversation id alignment, saved HTML `<p>持久化草稿验证第一段</p><p>第二段手工编辑内容</p>`, no placeholder attribute leakage, restored editor text after reload, contentEditable current paragraph still active, no desktop horizontal overflow, and 390x844 mobile shell/no-overflow behavior. | Preview deployed: `https://oases-chat-fju766qkv-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/6dX19LiEXeoaTkvaicZsvAF6wMt6`. |
| 2026-06-16 12:45 | Phase 42 completed: Word document mode now has a functional find/replace surface instead of a decorative search label. The titlebar search opens a panel, the Ribbon exposes a mobile-friendly 查找 button, `Cmd/Ctrl+F` opens find, `Cmd/Ctrl+H` opens replace, matches are highlighted in the editable document, previous/next navigation moves the active match, and 全部替换 mutates the document text and saves the draft. Search highlight `<mark>` elements are stripped by `cleanDocumentEditorHtml` before draft persistence and export. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright verified two `苹果` matches become highlighted with `1/2` count and one active match; replacing all with `香蕉` updates both document paragraphs; IndexedDB draft HTML becomes `<p>香蕉在第一段。</p><p>第二段也有香蕉。</p><p>没有关键词的一段。</p>`; no `<mark>`/`doc-find-match` leaks into storage; refreshing restores the replaced text with zero highlights; desktop has no horizontal overflow; mobile find panel opens from the Ribbon and fits within 390px width. | Preview deployed: `https://oases-chat-6kykdwfy1-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/6QedMTxQibTxJ385fRHwRCMdNvdm`. |
| 2026-06-16 12:58 | Phase 43 completed: Word document mode now supports inserting local images as document content. Insert > 图片 opens a dedicated `image/*` file input, validates a 6MB limit, reads the file as a data URL, inserts a `doc-image-figure` with `img[data-doc-image]` and caption, persists it in the Word draft, restores it after reload, and includes it in HTML / Word-compatible export output. This path is separate from chat attachments. | `pnpm test:unit` passed; `pnpm build` passed with known CSS minify/chunk warnings. Playwright inserted `/tmp/oases-word-insert-image.png`, verified one document image figure and caption, no desktop overflow, IndexedDB draft contains `doc-image-figure`, `data:image/png;base64,` and caption text, reload restores one image and caption, exported HTML contains the figure/data URL/caption, and mobile 390x844 Insert tab exposes the 图片 command without horizontal overflow. | Preview deployed: `https://oases-chat-3fw87jzci-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/8QYzU48AkDpkfP74VvkMzfzPN57p`. |
| 2026-06-16 13:03 | Phase 44 completed: restored the earliest model waiting animation as the shared pending renderer instead of leaving separate literal waiting text paths. Normal assistant pending messages now use the same helper, and Word document mode converts model/voice/ocli continuation placeholders into the no-label MatrixLoader paragraph. | `pnpm test:unit` passed; `pnpm build` passed with known CSS/chunk warnings. Browser base check confirmed the app loads with no console errors; focused Playwright delayed-response checks verified 16 MatrixLoader cells in normal chat, 16 no-label MatrixLoader cells inside `.word-editor-surface`, no waiting placeholder text in the Word body, and loader replacement after the streamed response. | Preview deployed: `https://oases-chat-6rsq54a92-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/2cM2wy7ChPSN3vYsiYUDw822EE3T`. |
| 2026-06-16 13:26 | Phase 45 completed: Word document images now behave like editable document objects. Inserted images receive stable ids, are selected on click or immediately after insertion, expose a contextual 图片格式 Ribbon group, can be resized to small/medium/large, aligned left/center/right, and deleted from the document. Ribbon group flex behavior was fixed so contextual tools no longer overlap the right-side common tools. | `pnpm test:unit` passed; `pnpm build` passed with known CSS/chunk warnings. Playwright verified inserted image selection, contextual toolbar visibility, large/right formatting, IndexedDB draft preserving `doc-image-size-large` and `doc-image-align-right` without `is-selected`, HTML export preserving formatting without selection leakage, reload restore without selected state, delete removing the figure from DOM and draft, and mobile 390x844 toolbar visibility with no horizontal overflow. | Preview deployed: `https://oases-chat-ak2c4q1ue-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/AN5caYFHXNtJsVyr4Kwo42ps5MbK`. |
| 2026-06-17 00:00 | Promoted Phase 45 Word image formatting build to production after user approval. | Local `pnpm build` passed before deploy with known CSS/chunk warnings. Vercel production build passed with the same known chunk warning and pnpm/esbuild script warning only. Deployment reports `READY`, target `production`, and alias `https://www.oasesai.xyz`. | Production deployed: `https://www.oasesai.xyz`; deployment URL: `https://oases-chat-mxaymha50-qingtengs-projects.vercel.app`; inspect: `https://vercel.com/qingtengs-projects/oases-chat/BQVuurQT3tRU4aewZ7qT9YRyPH85`. |
| 2026-06-17 11:35 | Phase 46 completed: `ocli` now treats packaged `OcliSkills` as a bundled skill source alongside workspace `.oases/skills`. `skill_list` returns both sources, `skill_read` can load built-in skills such as `web-search`, loaded skill context includes source/root/baseDir metadata, and the npm package manifest/test suite now verifies `OcliSkills` is included and readable after tarball install. Local package/runtime version prepared as `0.1.7`. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Build still has known CSS minify and chunk-size warnings. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 12:20 | Phase 47 completed: `ocli` now exposes read-only `skill_asset_list` and `skill_asset_read` tools so built-in or workspace skills can safely load their own `references/`, `scripts/`, `evals/`, and examples after `SKILL.md` is loaded. Asset reads are realpath-confined to the selected skill directory, capped at 512KB, and package smoke verifies the installed tarball can read `OcliSkills/web-search/scripts/browser_search.py`. `skill_loaded` audit metadata now preserves source/root. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Build still has known CSS minify and chunk-size warnings. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 12:45 | Phase 48 completed: `ocli` now exposes `skill_install`, which copies a bundled skill into the current workspace under `.oases/skills/<targetName>` for project/team customization. It validates target names, refuses to overwrite existing workspace skills, skips symlinks, copies scripts/references/evals, and records the installed `SKILL.md` as an artifact. Smoke tests verify installing `web-search`, reading the copied script as a workspace skill asset, duplicate-install rejection, and installed-tarball behavior. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Build still has known CSS minify and chunk-size warnings. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 13:15 | Phase 49 completed: workspace plugin discovery v1 added. `plugin_list` and `plugin_read` now recognize `.oases/plugins/<plugin>/.oases-plugin/plugin.json` and Claude-style `.claude-plugin/plugin.json`, parse manifest metadata, summarize README, commands, agents, and hooks, and reject manifest paths outside `.oases/plugins`. Smoke tests cover workspace plugin creation plus installed-tarball plugin discovery. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `pnpm build` passed. Build still has known CSS minify and chunk-size warnings. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 21:53 | Phase 50 completed: plugin command templates v1 added. `plugin_command_list` lists `.oases/plugins/<plugin>/commands/*.md` files, parses Markdown frontmatter plus first headings, and can filter by plugin. `plugin_command_read` reads a command by plugin/name or relative path, returns full/truncated content plus body/metadata, and rejects paths outside `.oases/plugins/*/commands`. Root and standalone runtime copies are synced; README files now include npm `E404 PUT` publish troubleshooting. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 22:00 | Phase 51 completed: plugin agent definitions v1 added. `plugin_agent_list` lists `.oases/plugins/<plugin>/agents/*.md`, filters by plugin, and parses the same frontmatter fields as workspace custom agents, including tools, skills, initialPrompt, effort, and agentType. `plugin_agent_read` reads by plugin/name or path, returns prompt/body/metadata with `source: "plugin"`, and rejects paths outside plugin agent directories. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. A parallel standalone run first hit `EADDRINUSE` on test port `8798`; serial rerun passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 22:08 | Phase 52 completed: plugin skill definitions v1 added. `plugin_list` now summarizes `.oases/plugins/<plugin>/skills/**/SKILL.md`, `plugin_skill_list` filters plugin skills and parses skill frontmatter, and `plugin_skill_read` reads by plugin/name or path with `source: "plugin"` while rejecting paths outside plugin skill directories. Plugin skills remain separate from global `skill_list` and are not auto-injected until install/injection rules are implemented. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 22:15 | Phase 53 completed: plugin asset resources v1 added. `plugin_asset_list` can list a whole plugin or subdirectory such as `skills/<skill>`, and `plugin_asset_read` can read references, scripts, examples, hooks, commands, agents, or skill resources as UTF-8 text. Reads are realpath-confined to the selected plugin root, reject `../` escapes, and cap single-file reads at 512KB. Script and hook files are read-only; execution still requires `run_command` or `run_python` and the normal approval path. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 22:23 | Phase 54 completed: workspace plugin install v1 added. `plugin_install` copies a workspace-local source plugin directory into `.oases/plugins/<targetName>` after verifying a `.claude-plugin/plugin.json` or `.oases-plugin/plugin.json` manifest. It refuses to overwrite existing plugin directories, skips `.git`, `node_modules`, `.DS_Store`, and symlinks, and immediately returns installed plugin metadata so `plugin_read` / `plugin_asset_read` can use the installed copy. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. The first root smoke found a macOS `/var` vs `/private/var` realpath return bug in `sourcePath`; fixed by returning the original workspace path before rerun. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 23:05 | Phase 55 completed: workspace plugin remove v1 added. `plugin_remove` removes an installed plugin by manifest name, plugin root, or manifest path, but only after verifying the target stays under `.oases/plugins` by realpath and still contains a supported `.claude-plugin/plugin.json` or `.oases-plugin/plugin.json` manifest. The tool is registered as `destructive`, has an explicit agent approval reason, reports removed plugin artifacts, and refuses non-plugin directories or path escapes. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 23:35 | Phase 56 completed: workspace plugin enable/disable v1 added. `plugin_disable` writes a `.oases-disabled` marker into an installed plugin directory and `plugin_enable` removes it. `plugin_list` now reports `enabled` / `disabled` plus `disabledPath`; plugin command, agent, and skill list tools hide disabled plugins by default and accept `includeDisabled: true` for management views. The tools validate the same installed-plugin manifest and `.oases/plugins` boundary as `plugin_remove`, and agent result summaries now show enable/disable events. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-17 23:55 | Phase 57 completed: plugin skill install v1 added. `plugin_skill_install` copies an installed plugin's skill directory from `.oases/plugins/<plugin>/skills/<skill>` into `.oases/skills/<targetName>`, preserving `SKILL.md` and nested resources such as `references/`. It refuses to overwrite existing workspace skills, verifies the source remains inside the selected plugin root, and lets the installed copy immediately work with normal `skill_read` and `skill_asset_read`. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 00:15 | Phase 58 completed: plugin agent install v1 added. `plugin_agent_install` copies an installed plugin's Markdown agent definition from `.oases/plugins/<plugin>/agents/<agent>.md` into `.oases/agents/<targetName>.md`. It refuses to overwrite existing workspace agents, validates the source stays inside the selected plugin root, and makes the installed copy visible through normal `agent_list` and `agent_read` so existing `agent_run(agentName)` can use it as a project custom agent. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 00:35 | Phase 59 completed: plugin command install v1 added. `command_list` and `command_read` now expose project-local `.oases/commands/*.md` command templates, while `plugin_command_install` copies an installed plugin command from `.oases/plugins/<plugin>/commands/<command>.md` into `.oases/commands/<targetName>.md`. The install path validates the plugin source boundary, refuses duplicate workspace command targets, and lets installed commands immediately appear through normal workspace command tools. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 00:06 | Phase 60 completed: plugin hook discovery v1 added. `plugin_hook_list` now lists plugin hook configs and handler files from both `hooks/` and `hooks-handlers/`, parses `hooks.json` descriptions, event names, matchers, hook command summaries, and classifies script handlers without executing them. `plugin_hook_read` reads config or handler source by plugin/name or path, returns parsed event metadata for JSON configs, rejects paths outside plugin hook directories, and follows the existing disabled-plugin filtering model with `includeDisabled: true` for management views. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 00:29 | Phase 61 completed: plugin manifest capability discovery v1 added. `plugin_capability_list` and `plugin_capability_read` now summarize plugin `mcpServers`, `lspServers`, manifest `settings`, custom component paths, `commandsMetadata`, `output-styles`, and `settings.json` structure without starting servers, applying settings, or executing plugin code. Sensitive setting keys such as token, secret, key, password, credential, authorization, and API key are returned as redacted shape metadata rather than raw values. Disabled plugins are hidden by default and can be included with `includeDisabled: true` for management views. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 00:58 | Phase 62 completed: plugin output styles v1 added. `output_style_list` and `output_style_read` now expose project-local `.oases/output-styles/*.{md,json}` templates. `plugin_output_style_list` and `plugin_output_style_read` discover/read plugin `output-styles/*.{md,json}` with Markdown frontmatter and heading metadata. `plugin_output_style_install` copies a selected plugin output style into `.oases/output-styles/<targetName>`, refuses duplicate targets, and keeps source validation inside the selected plugin root. Output styles are not automatically applied to model responses yet; this phase only makes them discoverable and installable for later UI/prompt orchestration. Local package/runtime version prepared as `0.1.8` because npm latest is already `0.1.7`. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. The first parallel root smoke hit `EADDRINUSE` because standalone smoke used the same local fake API port; serial rerun passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:09 | Phase 63 completed: explicit output style loading v1 added. `output_style_list`, `plugin_output_style_list`, and `plugin_output_style_install` still only discover or install style resources, but a successful agent-session `output_style_read` or `plugin_output_style_read` now activates that style for the rest of the run. `ocli` injects the loaded template into later model turns as `<output_style_context>`, de-duplicates by style path, emits `output_style_loaded`, records `activeOutputStyles`, and propagates active styles through sub-agent results. Agent sessions can also accept `preloadedOutputStyles` for Web-side orchestration later. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. The first parallel standalone smoke hit `EADDRINUSE` on the shared fake API port; serial rerun passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:18 | Phase 64 completed: workspace settings discovery v1 added. `settings_list` now discovers project `.oases/settings.json` and `.oases/settings.local.json` by default, with opt-in `.claude/settings.json` / `.claude/settings.local.json` compatibility audits through `includeClaude: true`. `settings_read` returns shape-only summaries, rejects `.claude` reads unless explicitly allowed, and redacts sensitive keys such as token, secret, key, password, credential, authorization, and API key. This phase intentionally does not apply settings, enable MCP/LSP, run hooks, or change permissions yet. Local package/runtime version prepared as `0.1.9` because npm latest is still `0.1.7`. | `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check ocli/src/constants.js`, `node --check oases-ocli/src/constants.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Package smoke was hardened to read expected help version from package.json instead of a hard-coded version. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:25 | Phase 65 completed: settings outputStyle activation v1 added. At agent session startup, `ocli` now reads only the whitelisted `outputStyle` field from `.oases/settings.json` and `.oases/settings.local.json`, with local settings taking precedence, then loads a matching workspace output style or plugin output style before the first model turn. The loaded style is injected as `<output_style_context>`, emits `output_style_loaded` with `settings: true`, and records `settingPath` / `settingsOutputStyle` in `activeOutputStyles`. Missing settings or missing styles do not fail the session; missing styles emit a `settings_warning`. Other settings remain audit-only and are not applied. Local package/runtime version prepared as `0.1.10` because npm latest is still `0.1.7`. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check oases-ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:33 | Phase 66 completed: settings permissions deny v1 added. At agent session startup, `ocli` now reads `.oases/settings.json` and `.oases/settings.local.json` `permissions.deny` arrays, parses Claude-style rules and Oases tool names, and blocks matching tools before approval or execution. Tool-wide deny rules are removed from the model tool schema; content rules such as `Write(denied-file.txt)` or `Bash(npm install)` stay in schema but are hard-blocked when arguments match. This phase intentionally does not apply `permissions.allow`, `permissions.ask`, or `defaultMode`, so settings can only reduce tool access, not silently grant broader execution. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies a denied `write_file` call returns a permissions.deny error and does not write the target file. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:41 | Phase 67 completed: settings permissions ask v1 added. At agent session startup, `ocli` now reads `.oases/settings.json` and `.oases/settings.local.json` `permissions.ask` arrays using the same Claude-style/Oases rule parser as deny. Matching calls are not hidden from the model and are not auto-allowed; instead they force an approval request with category `settings_permission_ask`, including normally non-prompting tools such as workspace-bounded `write_file`. `settings_permissions_loaded` and session results now include ask counts and asked tool metadata. `permissions.allow` and `defaultMode` remain audit-only. Local package/runtime version prepared as `0.1.12`. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies a settings-forced `write_file` approval, approval category/reason, successful approval resolution, ask metadata, and final file write. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 01:51 | Phase 68 completed: local settings permissions allow v1 added. At agent session startup, `ocli` now reads `.oases/settings.local.json` `permissions.allow` arrays using the shared permission rule parser. Matching allow rules skip default approval prompts only after `permissions.deny` and `permissions.ask` have had precedence, and committed `.oases/settings.json` allow rules do not skip approvals. Allowed calls emit `settings_permission_allowed` and session results include allow counts/tool metadata. Local package/runtime version prepared as `0.1.13`. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies a local allow rule skips `run_command` approval, emits allow audit metadata, records `settingsPermissions.allowedTools`, and still completes through the normal tool path. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 02:00 | Phase 69 completed: settings permissions defaultMode safe subset v1 added. At agent session startup, `ocli` now reads `.oases/settings.json` and `.oases/settings.local.json` `permissions.defaultMode`, with local settings taking precedence. Supported modes are `default`, `plan`, and `dontAsk`; unsupported permission-widening modes such as `acceptEdits` and `bypassPermissions` emit `settings_warning` and are not applied. `plan` blocks write/execution/destructive/agent tools while allowing read/network planning tools and `todo_write`. `dontAsk` denies approval-required calls unless a local allow rule pre-approves them. Local package/runtime version prepared as `0.1.14`. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies plan-mode blocks `write_file` without approval or file writes, dontAsk denies default approval-required `run_command` without approval, and settings result metadata records default modes. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 02:09 | Phase 70 completed: explicit command template loading v1 added. `command_list`, `plugin_command_list`, and `plugin_command_install` still only discover or install templates, but a successful agent-session `command_read` or `plugin_command_read` now activates that command template for the rest of the run. `ocli` injects the loaded template into later model turns as `<command_context>`, de-duplicates by command path, emits `command_loaded`, records `activeCommands`, and propagates active command metadata through sub-agent results. Command templates remain prompt/workflow templates, not executable code. Local package/runtime version prepared as `0.1.15`. | `node --check ocli/src/agent.js`, `node --check oases-ocli/src/agent.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies `command_read` loads a project command template, emits `command_loaded`, records `activeCommands`, injects `<command_context>`, and uses that context to write the guided output file. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 02:27 | Phase 71 completed: preloaded command templates v1 added. Agent sessions now accept `preloadedCommands` so Web or a future command palette can inject command templates before the first model turn. Workspace custom agents now parse `commands` frontmatter alongside `skills`; `agent_run({ agentName })` reads matching workspace or plugin commands before the sub-agent starts, passes them into the child run, emits nested `command_loaded(preloaded:true)`, and returns `activeCommands` through the `agent_run` result. Local package/runtime version prepared as `0.1.16`. | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/src/agent.js`, `node --check oases-ocli/src/tools.js`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` passed. Smoke verifies direct `preloadedCommands` injection without a `command_read` tool call, and custom agent `commands: review-flow` preload into a sub-agent before its first model request. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 02:35 | Phase 72 completed: Web command template picker v1 added. Desktop Web project mode now silently discovers connected `ocli` workspace commands through `command_list` and plugin commands through `plugin_command_list`, shows a compact command-template selector in the project composer and workspace panel, and reads the selected template at send time with `command_read` or `plugin_command_read`. The selected template is sent as `preloadedCommands` for both `/agent/sessions` and legacy `/agent/run`, so `ocli` can inject `<command_context>` before the first model turn. Project-mode system prompts and tool event rendering now include `command_list`, `command_read`, `plugin_command_list`, `plugin_command_read`, and `plugin_command_install`; disconnected browser-only project mode explicitly fails these local-only tools instead of falling through to virtual workspace behavior. | `pnpm build` passed with the existing CSS minify and chunk-size warnings. No CLI runtime files changed in this phase, so `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` were not rerun. | Not deployed yet; Web code changed but user has not requested a Vercel deployment for this phase. |
| 2026-06-18 02:35 | Phase 73 completed: Web output style picker v1 added. Desktop Web project mode now also discovers connected `ocli` workspace output styles through `output_style_list` and plugin output styles through `plugin_output_style_list`, shows a compact output-style selector next to the command-template selector, and reads the selected style at send time with `output_style_read` or `plugin_output_style_read`. The selected style is sent as `preloadedOutputStyles` for both `/agent/sessions` and legacy `/agent/run`, allowing `ocli` to inject `<output_style_context>` before the first model turn. The project workbench strip was changed from a rigid 3-column grid to a wrapping flex layout so command/style selectors do not overflow. Project-mode prompts and tool rendering now include `output_style_list`, `output_style_read`, `plugin_output_style_list`, `plugin_output_style_read`, and `plugin_output_style_install`; disconnected browser-only project mode explicitly fails these local-only tools. | `pnpm build` passed with the existing CSS minify and chunk-size warnings. No CLI runtime files changed in this phase, so `pnpm test:ocli`, `pnpm test:ocli-package`, and `npm test --prefix oases-ocli` were not rerun. | Not deployed yet; Web code changed but user has not requested a Vercel deployment for this phase. |
| 2026-06-18 02:54 | Phase 74 completed: project memory tools v1 added. `memory_list`, `memory_read`, and `memory_write` now manage conservative Markdown memory files under `.oases/memory/project`, `.oases/memory/team`, and `.oases/memory/private`. Paths are restricted to single-level `*.md` files, `memory_read` rejects non-memory paths and overlarge files, and `memory_write` refuses overwrite unless `overwrite: true` is explicit. This is deliberately only file management: no automatic memory prompt injection and no implicit conversation saving yet. Local package/runtime version prepared as `0.1.17`. | `node --check ocli/src/tools.js`, `node --check ocli/src/constants.js`, `node --check scripts/ocli-smoke.mjs`, `node --check scripts/ocli-package-smoke.mjs`, `node --check oases-ocli/src/tools.js`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `node --check oases-ocli/scripts/ocli-package-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `npm pack --dry-run` from `oases-ocli` passed. Smoke verifies registry exposure, memory write/list/read, duplicate overwrite rejection, non-memory path rejection, nested path rejection, and installed-tarball memory behavior. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-18 03:03 | Phase 75 completed: explicit memory loading v1 added. A successful agent-session `memory_read` now activates that memory for later turns by injecting `<memory_context>`, emitting `memory_loaded`, and recording `activeMemories`. Agent sessions can also receive `preloadedMemories`, and workspace custom agents can declare `memories: project:name, team:name, ...` to preload selected memory before a sub-agent starts. This remains opt-in memory loading only: no automatic bulk memory scan and no implicit conversation saving. Local package/runtime version prepared as `0.1.18`. | `node --check ocli/src/agent.js`, `node --check ocli/src/tools.js`, `node --check scripts/ocli-smoke.mjs`, `node --check oases-ocli/src/agent.js`, `node --check oases-ocli/src/tools.js`, `node --check oases-ocli/scripts/ocli-smoke.mjs`, `pnpm test:ocli`, `pnpm test:ocli-package`, `npm test --prefix oases-ocli`, and `npm pack --dry-run` from `oases-ocli` passed. Smoke verifies explicit `memory_read` context injection, preloaded memory context injection without a tool call, `memory_loaded` events, `activeMemories` metadata, and custom-agent `memories` frontmatter parsing. No Web files changed, so no Vercel deploy was needed. | Not deployed; local CLI/package/docs change only. |
| 2026-06-19 13:20 | Phase 76 completed: Web memory picker added to project mode. `memory_list` discovery via `useEffect` + `POST /tools/memory_list`, `selectedLocalCliMemoryId` state, `readSelectedLocalCliPreloadedMemory` reader, `Brain`-icon `<select>` in desktop workbench strip, mobile workspace panel memory list selector + badge, i18n for zh-CN and en, `buildProjectSystemPrompt` now mentions `memory_list`/`memory_read`/`memory_write` and `<memory_context>` injection, disconnected error handler covers memory tools, `preloadedMemories` wired to session and legacy agent endpoints. | `pnpm build` TypeScript compilation passed | Production deployed |
| 2026-06-19 13:31 | Phase 77 completed: native `web_search` tool added to ocli. Uses DuckDuckGo HTML API (no API key required) to search the web and return structured results with titles, URLs, and snippets. `webSearch` function in `network.js`, tool definition in `tools.js`, registered in `PROJECT_TOOL_NAMES`, Web UI type/routing/system prompt/disconnected error handler all updated, synced to `oases-ocli/`. | `pnpm build` TypeScript compilation passed | Production deployed |
| 2026-06-19 13:48 | Phase 78 completed: `todo_read` tool added to read `.oases/todo.json` from workspace; returns todos array, counts, summary, and path. Falls back gracefully with a note when no todo file exists. Phase 6 (Skills, agents, plugins) marked Done in migration plan. `VERSION` bumped to `0.1.19`. | `pnpm build` passed | Production deployed |
| 2026-06-19 14:08 | Phase 79 completed: MCP client bridge v1 added. Zero-dependency MCP client (`mcpClient.js`) using JSON-RPC 2.0 over stdio child processes. Reads `mcpServers` from `.oases/settings.json` and `.oases/settings.local.json`. Exposes `mcp_list` (list tools from all configured servers), `mcp_call` (call a tool on a specific server), `mcp_resources_list` (list resources), and `mcp_resource_read` (read a resource by URI). All four tools registered in `PROJECT_TOOL_NAMES`, Web UI type/routing/risk/success/system prompt updated, synced to `oases-ocli/`. | `pnpm build` passed | Production deployed |

## Latest Known Good Validation

```text
pnpm test:ocli          passed
pnpm test:ocli-package  passed
npm test --prefix oases-ocli passed
pnpm test:unit          passed
pnpm build              passed with known CSS minify and chunk-size warnings
Browser QA              passed for Phase 36 real document editing/export and Phase 35 document disguise empty-document spacing/layout
Browser/Playwright QA   passed for Phase 37 Word-style AI editing disguise desktop and mobile checks
Playwright QA           passed for Phase 38 full Word paragraph disguise desktop mocked-submit and mobile layout checks
Playwright QA           passed for Phase 39 delayed-stream Word loader replacement check
Playwright QA           passed for Phase 40 contentEditable Word current-paragraph input desktop/mobile checks
Playwright QA           passed for Phase 41 IndexedDB Word draft persistence refresh-restore checks
Playwright QA           passed for Phase 42 Word find/replace and highlight-clean persistence checks
Playwright QA           passed for Phase 43 Word inline image insertion, persistence, export, and mobile Insert checks
Playwright QA           passed for Phase 44 unified normal/Word delayed-loader checks
Playwright QA           passed for Phase 45 Word image formatting, persistence, export, delete, and mobile toolbar checks
ocli bundled skills     passed for Phase 46 multi-source skill_list/skill_read and installed tarball bundled web-search read
ocli skill assets       passed for Phase 47 skill_asset_list/skill_asset_read, path boundary rejection, and installed tarball bundled asset read
ocli skill install      passed for Phase 48 bundled skill installation, duplicate refusal, copied script asset read, and installed tarball install smoke
ocli plugins            passed for Phase 49 workspace plugin_list/plugin_read manifest, README, commands, agents, hooks, path rejection, and installed tarball plugin discovery
ocli plugin commands    passed for Phase 50 plugin_command_list/plugin_command_read metadata parsing, command body reads, path rejection, and installed tarball command reads
ocli plugin agents      passed for Phase 51 plugin_agent_list/plugin_agent_read frontmatter parsing, prompt reads, path rejection, and installed tarball agent reads
ocli plugin skills      passed for Phase 52 plugin_skill_list/plugin_skill_read frontmatter parsing, SKILL.md reads, path rejection, and installed tarball skill reads
ocli plugin assets      passed for Phase 53 plugin_asset_list/plugin_asset_read nested references/scripts reads, path rejection, and installed tarball asset reads
ocli plugin install     passed for Phase 54 workspace-local plugin_install copy, manifest validation, duplicate refusal, installed plugin read/assets, and installed tarball install smoke
ocli plugin remove      passed for Phase 55 workspace plugin_remove deletion, manifest validation, non-plugin/path rejection, destructive metadata, and installed tarball remove smoke
ocli plugin enablement  passed for Phase 56 plugin_disable/plugin_enable marker writes, plugin_list state, disabled filtering, includeDisabled opt-in, and installed tarball enable/disable smoke
ocli plugin skill install passed for Phase 57 plugin_skill_install copying SKILL.md and references into .oases/skills, duplicate rejection, workspace skill reads, and installed tarball skill install smoke
ocli plugin agent install passed for Phase 58 plugin_agent_install copying plugin agent Markdown into .oases/agents, duplicate rejection, workspace agent reads, and installed tarball agent install smoke
ocli plugin command install passed for Phase 59 plugin_command_install copying plugin command Markdown into .oases/commands, duplicate rejection, workspace command reads, and installed tarball command install smoke
ocli plugin hooks       passed for Phase 60 plugin_hook_list/plugin_hook_read hooks.json event parsing, handler source reads, disabled filtering, and installed tarball hook reads
ocli plugin capabilities passed for Phase 61 plugin_capability_list/plugin_capability_read MCP/LSP/settings/path metadata summaries, sensitive setting redaction, disabled filtering, and installed tarball capability reads
ocli plugin output styles passed for Phase 62 output_style_list/output_style_read plus plugin_output_style_list/plugin_output_style_read/plugin_output_style_install metadata parsing, install duplicate rejection, disabled filtering, and installed tarball output-style reads
ocli explicit output styles passed for Phase 63 output_style_read/plugin_output_style_read activation, output_style_loaded events, activeOutputStyles metadata, output_style_context prompt injection, and installed tarball output-style activation smoke
ocli workspace settings passed for Phase 64 settings_list/settings_read .oases discovery, opt-in .claude compatibility audit, sensitive setting redaction, and installed tarball settings smoke
ocli settings outputStyle passed for Phase 65 automatic .oases/settings outputStyle preload, settings.local precedence path support, output_style_loaded(settings:true) metadata, and no model-side output_style_read requirement
ocli settings permissions deny passed for Phase 66 permissions.deny rule parsing, content-fragment deny checks, settings_permissions_loaded events, settingsPermissions metadata, and blocked write_file no-write verification
ocli settings permissions ask passed for Phase 67 permissions.ask rule parsing, forced write_file approval, settings_permission_ask category, settings_permissions_loaded ask metadata, settingsPermissions askedTools metadata, and approved write verification
ocli local settings permissions allow passed for Phase 68 permissions.allow rule parsing from .oases/settings.local.json, default approval skip, settings_permission_allowed audit event, settingsPermissions allowedTools metadata, and allowed run_command verification
ocli settings permissions defaultMode passed for Phase 69 plan-mode write blocking, dontAsk approval-required command denial, no approval prompts in either mode, no blocked file writes, and settingsPermissions defaultMode metadata
ocli explicit command templates passed for Phase 70 command_read/plugin_command_read activation, command_loaded events, activeCommands metadata, command_context prompt injection, and guided command smoke
ocli preloaded command templates passed for Phase 71 direct preloadedCommands injection, custom agent commands frontmatter parsing, nested command_loaded(preloaded:true), activeCommands propagation, and no command_read requirement for preloaded sessions
Web command picker      passed for Phase 72 command_list/plugin_command_list discovery, selected command_read/plugin_command_read preload payload, and project tool rendering via pnpm build
Web output style picker passed for Phase 73 output_style_list/plugin_output_style_list discovery, selected output_style_read/plugin_output_style_read preload payload, wrapping project workbench strip, and project tool rendering via pnpm build
ocli project memory    passed for Phase 74 memory_list/memory_read/memory_write metadata, scoped Markdown path policy, duplicate overwrite rejection, nested/non-memory path rejection, and installed tarball memory smoke
ocli explicit memory   passed for Phase 75 memory_read activation, memory_loaded events, activeMemories metadata, memory_context prompt injection, preloadedMemories injection, and custom agent memories frontmatter parsing
Browser QA              passed for Phase 34 Web polish, About/Terms routes, light mode, and mobile no-overflow checks
Focused Playwright      passed for IndexedDB-backed model-switch recovery
Focused Playwright      passed for Phase 33 model spec suite
npm install -g oases-ocli passed in clean temp prefix
npm view oases-ocli version dist-tags reports latest 0.1.7
local oases-ocli package version is 0.1.24
Local Browser QA        passed for Phase 28 Web project-mode UX
Local HTTP auth smoke   passed for Phase 30 token-gated ocli
```

Latest production deployment:

```text
https://www.oasesai.xyz
https://oases-chat-bdi5pa3ja-qingtengs-projects.vercel.app
Inspect: https://vercel.com/qingtengs-projects/oases-chat/6UwYfRDi617AQL4jafp5XHm4kDcY
```

Latest preview deployment:

```text
https://oases-chat-fvdwizdy0-qingtengs-projects.vercel.app
Inspect: https://vercel.com/qingtengs-projects/oases-chat/6vvLrNGKwcNAvKGnu2rXKYX9CYe3
Note: direct unauthenticated curl redirects to Vercel SSO because Preview protection is enabled.
```
ocli Web memory picker  passed for Phase 76 memory_list discovery, Brain-icon selectors, i18n, system prompt memory mentions, and pnpm build TypeScript compilation
ocli native web_search    passed for Phase 77 DuckDuckGo search, webSearch export, tool definition, Web UI routing, system prompt, and pnpm build
ocli todo_read + version bump passed for Phase 78 todo_read tool, PROJECT_TOOL_NAMES, Web UI routing, and pnpm build
ocli MCP client bridge  passed for Phase 79 mcpClient.js, mcp_list/mcp_call/mcp_resources_list/mcp_resource_read tools, Web UI routing, and pnpm build
ocli auto MCP memory evidence passed for Phase 80 MCP Evidence memory content, mcpResultCount metadata, Web memory card badge, root/standalone smoke, build, unit, and focused Playwright checks
ocli context compaction MCP result snapshot passed for Phase 81 autoMcpResults state snapshot preservation, root/standalone smoke, package smoke, and build checks
ocli custom agent MCP scoping passed for Phase 82 custom-agent mcpTools/disallowedMcpTools parsing, agent_write metadata, runtime mcp_call allow/deny enforcement, root/standalone smoke, package smoke, and build checks
ocli agent framework MCP dependencies passed for Phase 83 framework mcpTools/mcpServers resolution, mcp_context injection, capabilityRouting metadata, root/standalone smoke, package smoke, and build checks
ocli agent framework MCP resources passed for Phase 84 framework mcpResources persistence, mcp_resources_list resolution, mcp_context resource injection, capabilityRouting metadata, active framework metadata, root/standalone smoke, package smoke, and build checks
ocli auto-memory routing evidence passed for Phase 85 Routing Evidence memory section, framework/MCP resource links, evidence counts, root/standalone smoke, package smoke, and build checks
ocli Web request policy + Framework MCP surface passed for Phase 86 Kimi/GPT request normalization, proxy stale-request cleanup, Framework MCP server/tool/resource summaries, unit/build, focused Playwright, Browser QA, production HTTP 200, production Kimi stale-request smoke, and runtime-error checks
ocli adaptive context compaction passed for Phase 87 oversized recent-tail shrinkage, retained-count telemetry, root/standalone smoke, package smoke, and build checks
ocli Web compaction telemetry surface passed for Phase 88 adaptive retained-count display, Browser QA with ocli mock, unit tests, build checks, production stale Kimi request smoke, and deployment `dpl_3PLNoh1cgSk1yua3vEdCiABVw9FM`
ocli Agent Framework execution blueprint passed for Phase 89 agentRoles/handoffs/verificationGates metadata, framework_execution_blueprint prompt injection, active framework metadata, auto-memory/RAG evidence, root/standalone smoke, package smoke, and build checks
ocli Web Agent Framework blueprint surface passed for Phase 90 Web template/session/event/detail/memory prompt handling of agentRoles/handoffs/verificationGates, unit tests, build, focused desktop Playwright checks, production HTTP 200, production JS marker check, Vercel runtime-error scan, and deployment `dpl_G3T9xX9RvMRQ3jbqzBSg7VygNMnf`
ocli Framework blueprint execution guard passed for Phase 91 required Framework reviewer agent_run evidence before final response, framework_blueprint_guard auto_continue event/result metadata, root/standalone smoke, package smoke, build checks, and local package version bump to 0.1.20
ocli Web Framework guard telemetry passed for Phase 92 Framework blueprint guard event cards and session detail guard evidence, unit tests, build, focused desktop Playwright checks, production HTTP 200, production JS marker check, Vercel runtime-error scan, and deployment `dpl_2Bw5vGY3ffJ64pYFM5x3fewiQ6G6`
ocli Framework guard auto-delegation passed for Phase 93 deterministic missing-agent agent_run from Framework blueprint guard, automatic tool_start/tool_result telemetry, guided-smoke capability-routing isolation, root/standalone smoke, package smoke, build checks, and local package version bump to 0.1.21
ocli auto memory RAG evidence passed for Phase 94 autoMemoryResults routing metadata, memory_auto_searched SSE telemetry, Memory RAG Evidence memory-maintenance section, compaction state snapshot preservation, Web Auto memory RAG session detail panel, root/standalone smoke, package smoke, unit tests, build, focused desktop Playwright checks, production HTTP 200, production JS marker check, Vercel runtime-error scan, and deployment `dpl_8HrFscHYyEk16yDJn5xWCcFTT3B8`
ocli persistent todo restore passed for Phase 95 persisted `.oases/todo.json` auto-read, open-todo prompt injection, `todo_state_loaded` / `todo_state_updated` Web telemetry, completion-guard continuation from restored open todos, session-detail auto-scroll, root/standalone smoke, package smoke, unit tests, build, focused desktop Playwright checks, production HTTP 200, production JS marker check, Vercel runtime-error scan, and deployment `dpl_FWR2LoDrHYksWu6ebCUYc7pn6iEX`
ocli todo-aware context compaction passed for Phase 96 restored `latestTodoState` propagation into context-compaction snapshots, open-todo auto-continuation after compaction, final todo metadata preservation, Web session-detail fallback from `result.latestTodos/openTodos/todoCounts`, root/standalone smoke, package smoke, unit tests, build, focused desktop Playwright checks, production HTTP 200, production JS marker check, Vercel runtime-error scan, and deployment `dpl_B5Rhh2zTWK8ZjZFjHeqewrcLqM3b`
ocli structured resume continuity passed for Phase 97 `resumeContext` injection, `<session_resume_context>` preservation across compaction snapshots, injected resume/compaction context exclusion from auto-routing current-task detection, MCP/RAG/compaction evidence resume smoke, root/standalone smoke, and package smoke checks; local CLI/package/docs only, no Web deployment required
ocli recursive resume compaction passed for Phase 98 nested `sessionResumeContext` recovery from prior `<context_state_snapshot>` entries, MCP/RAG/todo evidence preservation across repeated compactions after the raw resume context has been compacted away, root/standalone smoke, and package smoke checks; local CLI/package/docs only, no Web deployment required
ocli clean auto-memory request capture passed for Phase 99 automatic wiki memory suggestions now ignore injected routing/MCP/session/compaction contexts and tool-result messages when deriving `title` and `## User Request`, preserving the real user intent for self-updating memory/RAG; root/standalone smoke and package smoke checks passed; local CLI/package/docs only, no Web deployment required
ocli post-tool continuation guard passed for Phase 100 tool-producing assistant turns that still promise follow-up work now append an explicit auto-continuation prompt and emit `auto_continue(afterToolResults:true)`, preventing Ocli from summarizing prematurely after the first tool result; root/standalone smoke and package smoke checks passed; local CLI/package/docs only, no Web deployment required
ocli Web post-tool continuation telemetry passed for Phase 101 Web project-mode timelines now render generic `auto_continue` events, distinguish `afterToolResults:true` with a 工具后续跑 badge, and keep Framework blueprint guard cards specialized; `npm run build` and focused desktop Playwright routing/compaction telemetry check passed; production deployment `dpl_FTUez3akh9BeZK1zVvPEFvEL8bFW` is Ready and aliased to `https://www.oasesai.xyz`; production HTTP 200, deployed JS marker check, Vercel inspect, and 10-minute log scan passed
ocli capability routing diagnostics passed for Phase 102 route decisions now persist query terms, candidate counts, thresholds, top selected/rejected samples, SSE event diagnostics, Web Routing diagnostics session-detail panel, valid JSON compaction/resume preservation, and package/runtime version 0.1.22; node --check, root smoke, package smoke, standalone package test, unit tests, build, and focused desktop Playwright session-detail checks passed; production deployment `dpl_AtK4C2dxEn1HKFMx9hctbSVWehgQ` is Ready and aliased to `https://www.oasesai.xyz`; production HTTP 200, deployed JS marker check, Vercel inspect, stale Kimi smoke, and runtime-error scan passed
ocli todo-aware auto-memory continuity passed for Phase 103 automatic wiki/RAG memory suggestions now preserve continuation state, todo evidence, open todo evidence, context-compaction evidence, and `stoppedReason`; session resume payloads carry `memoryMaintenance`; Web session detail displays open/compaction/continuation/stopped badges; node --check, root smoke, package smoke, standalone package test, unit tests, build, focused desktop Playwright, full e2e, production HTTP 200, deployed JS marker check, production Kimi smoke, Vercel READY inspect/list, runtime-error check, and status-code log check passed; production deployment `dpl_3ji2D1JBhguA1isKghkHtnVoe2Jz` is Ready and aliased to `https://www.oasesai.xyz`
ocli sub-agent evidence memory continuity passed for Phase 104 delegated `agent_run` and `agent_status` results now survive auto-memory suggestions, context-compaction snapshots, session resume context, and Web session detail; memory evidence records `subAgentCount`, Web renders Sub-agents and memory badges, root/standalone smoke verifies `## Sub-agent Evidence`, full Web e2e passed, production HTTP 200 and Kimi smoke returned 200 without invalid temperature, Vercel inspect/log checks passed, and production deployment `dpl_Cq4fbRwc3zgNyLk6CQt2GRHYrmyn` is Ready and aliased to `https://www.oasesai.xyz`
ocli adaptive routing live telemetry passed for Phase 105 Web project-mode timelines now distinguish initial automatic routing from adaptive routing, adaptive skill loads, adaptive memory RAG searches, adaptive MCP context loads, and adaptive MCP auto-calls; npm run build, Browser page-health QA, focused desktop Playwright, unit tests, full e2e, production HTTP 200, deployed JS marker check, production stale Kimi request smoke, Vercel inspect/log/error checks passed, and production deployment `dpl_AzJrLppyCSmxDfGsubUnRoXsS9ps` is Ready and aliased to `https://www.oasesai.xyz`
ocli external context adaptive routing passed for Phase 106 fetch_url and web_search results now trigger post-tool adaptive routing while adaptive routing queries are narrowed to routing-safe context results, preventing same-turn todo/write/execution output from selecting unrelated skills, memories, or MCP capabilities; node --check, focused adaptive routing unit test, full unit tests, root smoke, package smoke, standalone package test, package dry-run, and build checks passed; local CLI/package/docs change only, no Vercel deploy required; package/runtime version prepared as 0.1.23
ocli settings capability routing policy passed for Phase 107 project settings now control automatic capability routing enabled/adaptive/includeAgents/autoMcpCalls flags, selection limits, discovery limits, memory RAG search limits, and minimum scores; routing diagnostics, settings events, final result metadata, settings_read safeValues, and settings_write allowlists preserve the active policy; node --check, focused routing policy unit test, full unit tests, root smoke, package smoke, standalone package test, package dry-run, and build checks passed; local CLI/package/docs change only, no Vercel deploy required; package/runtime version prepared as 0.1.24
ocli Web capability routing policy surface passed for Phase 108 Web project-mode session detail now normalizes and renders settingsCapabilityRouting / diagnostics policy telemetry, including enabled/adaptive/includeAgents/autoMcpCalls toggles, settings source paths, selection limits, discovery pools, memory RAG limits, and minimum scores; settings_capability_routing_loaded SSE events now render as project tool context; full unit tests, production build, focused desktop Playwright session-detail check, Browser QA with local Ocli policy mock, production HTTP 200, deployed JS marker check, stale Kimi smoke, and runtime-error scan passed; production deployment `dpl_EViXfvdTker3mN3mjjHYDzTZxM1t` is Ready and aliased to `https://www.oasesai.xyz`
ocli settings context compaction policy passed for Phase 109 project settings now control automatic context compaction enabled/maxContextTokens/ratio/recentMessages defaults; request body overrides/disable still work; settings_context_compaction_loaded, context_compacted.policy, context-state snapshots, final result metadata, settings_read safeValues, and settings_write allowlists preserve the active policy; node --check, root smoke, package smoke, standalone package test, unit tests, build, and oases-ocli package dry-run passed; local CLI/package/docs/Web prompt change only, no Vercel deploy required; package/runtime version prepared as 0.1.25
ocli Web context compaction policy telemetry passed for Phase 110 Web project-mode live timeline and session detail now normalize and render settingsContextCompaction / contextCompactions[*].policy telemetry, including settings source paths, max context tokens, threshold tokens, ratio, recent-message defaults, settings_context_compaction_loaded SSE cards, and context_compacted policy previews; npm run build, unit tests, focused desktop Playwright routing/compaction and session-detail checks, production HTTP 200, deployed JS marker check, and Vercel READY inspection passed; production deployment `dpl_Hy7KvKxsMQiSUgPWJbxARnwWFZBc` is Ready and aliased to `https://www.oasesai.xyz`
ocli Web context compaction settings editor passed for Phase 111 project workbench now reads safeValues.contextCompaction from .oases/settings.local.json, renders enable/max tokens/trigger ratio/recent messages controls, and persists contextCompaction through settings_write; npm run build, unit tests, focused desktop Playwright memory-settings regression and context-compaction settings editor checks, production HTTP 200, deployed JS marker check, and Vercel READY inspection passed; production deployment `dpl_3wLoKxFFucAESaKRVmCKPXgEbFJB` is Ready and aliased to `https://www.oasesai.xyz`
ocli Web capability routing settings editor passed for Phase 112 project workbench now reads safeValues.capabilityRouting from .oases/settings.local.json, renders automatic routing toggles, selection limits, Memory RAG limits, and minimum score thresholds, and persists capabilityRouting through settings_write; npm run build, unit tests, focused desktop Playwright context-compaction settings regression and capability-routing settings editor checks, production HTTP 200, deployed JS marker check, and Vercel READY inspection passed; production deployment `dpl_6UwYfRDi617AQL4jafp5XHm4kDcY` is Ready and aliased to `https://www.oasesai.xyz`
ocli Web memory policy settings editor passed for Phase 113 project workbench now reads safeValues.memory from .oases/settings.local.json, renders auto-suggest, auto-write, and write-scope controls, preserves immediate auto-write toggling, and persists memory policy through settings_write; npm run build, unit tests, focused desktop Playwright memory-policy/context-compaction/capability-routing checks, production HTTP 200, deployed JS marker check, Vercel READY inspection, and 30-minute runtime-error log check passed; production deployment `dpl_5Kk5GAyuVZgY8WmqKob3mSYPHPNU` is Ready and aliased to `https://www.oasesai.xyz`
ocli Web Agent Framework composer passed for Phase 114 project workbench can create reusable multi-agent frameworks through agent_framework_write, including agents, skills, commands, memories, MCP servers/tools/resources, routing terms, roles, handoffs, and verification gates; saved frameworks are selected immediately and preloaded through the existing agent_framework_read session path; npm run build, unit tests, focused desktop Playwright Agent/Framework/routing/settings checks, production HTTP 200, deployed JS marker check, Vercel READY inspection, and 30-minute runtime-error log check passed; production deployment `dpl_62oTBgkjo5hSac1SQxYLtxScWFij` is Ready and aliased to `https://www.oasesai.xyz`
ocli Web MCP server settings editor passed for Phase 115 settings_read now exposes safe MCP server summaries, settings_write merges individual mcpServers patches without wiping existing env, and Web project workbench can inspect/edit/add/delete stdio MCP command/args declarations for automatic MCP routing; node --check, root smoke, package smoke, build, unit tests, focused desktop Playwright Agent Framework/settings regressions, and MCP settings editor check passed; preview deployment `dpl_6vvLrNGKwcNAvKGnu2rXKYX9CYe3` is Ready at `https://oases-chat-fvdwizdy0-qingtengs-projects.vercel.app`
ocli Web MCP discovery surface passed for Phase 116 project workbench can run mcp_list and mcp_resources_list from the MCP settings panel, render available tools/resources/input-schema keys/server counts, and surface per-server discovery errors without persisting dynamic discovery results; npm run build, unit tests, focused desktop Playwright MCP discovery check, broader Agent Framework/memory/routing/context-compaction/MCP settings regressions, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed; preview deployment `dpl_EHg9BDFZJuvtMfXtTqxffSUC5nzn` is Ready at `https://oases-chat-ka3qdm7ct-qingtengs-projects.vercel.app`
ocli Web Memory RAG search surface passed for Phase 117 project workbench can call memory_search from the Memory RAG panel, pass query/scope and routing-policy memorySearch limits, and render ranked snippets, matched terms, outgoing links, and backlinks; npm run build, unit tests, focused desktop Playwright Memory RAG check, broader Agent Framework/memory/routing/context-compaction/MCP settings regressions, Vercel preview deploy, and deployment inspection passed; preview deployment `dpl_ECscurPCRRU28ZgKhmYhMkzcNDxd` is Ready at `https://oases-chat-mazgxnpgc-qingtengs-projects.vercel.app`
ocli Web/Ocli capability route preview passed for Phase 118 Ocli now exposes read-only capability_route_preview and Web project workbench can preview automatic routing before a long run, including selected skills, commands, Memory RAG memories, agents, Agent Frameworks, MCP tools/resources, scores, matched terms, candidate counts, policy sources, framework gates, and MCP schema keys; node --check, direct handleTool route-preview smoke, npx tsc --noEmit, build, unit tests, root/package Ocli smokes, focused desktop Playwright route-preview/capability-routing checks, broader workbench regressions, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed; preview deployment `dpl_CWoLfaYsGCBKdiMsUBoBcpbqAvR8` is Ready at `https://oases-chat-r5t6tr13c-qingtengs-projects.vercel.app`
ocli preview-comparable routing snapshots passed for Phase 119 real agent routing diagnostics and capability_route_preview now share compact snapshot/fingerprint telemetry; Web renders route snapshot fingerprints in session detail and route preview panels; node --check for both runtime copies and smoke scripts, direct route-preview snapshot smoke, npx tsc --noEmit, unit tests, build, root/package Ocli smokes, standalone package tests, focused desktop Playwright route-preview/session-detail/routing checks, and broader workbench regressions passed; local Web/Ocli/docs change only, preview deployment pending
ocli Web route preview/session comparison passed for Phase 120 session detail now compares the latest real routing snapshot against the latest Route Preview snapshot, renders matches as `matches latest preview`, renders drift as `differs from latest preview`, and shows shared selected-key counts plus selected/candidate deltas; npx tsc --noEmit, unit tests, npm run build, focused Route Preview regression, focused desktop Playwright route-preview/session-detail/automatic-routing checks, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed; preview deployment `dpl_3RE2r9YBvhLGrwsFQYMNGvXWyyo7` is Ready at `https://oases-chat-8gitazp2u-qingtengs-projects.vercel.app`
ocli Web resume readiness passed for Phase 121 session detail now renders a Resume readiness panel with source session, prompt readiness, open todo, compaction, auto-continuation, file evidence, and stopped reason badges; clicking Continue calls the existing resume endpoint, inserts the resumed session, and opens the new detail with session_resumed telemetry; npx tsc --noEmit, unit tests, npm run build, focused session-detail resume e2e, focused desktop Playwright route-preview/session-detail/automatic-routing checks, Vercel preview deploy, protected root HTTP 200 fetch, and deployment inspection passed; preview deployment `dpl_8BrkHYQtCVSuey6BzQJf5GTfiUhe` is Ready at `https://oases-chat-9njth6tl3-qingtengs-projects.vercel.app`
ocli Web session approval blockers passed for Phase 122 session detail now preserves approval event fields, renders pending `Approval blockers` with tool/risk/category/reason/arguments, and approves/rejects through the session approval endpoint before reloading detail; npx tsc --noEmit, npm run build, unit tests, focused desktop Playwright session-detail/approval-blocker/live-approval checks, Browser QA with mock Ocli approval flow and no console warnings/errors, and Vercel deployment READY inspection passed; preview deployment `dpl_HxyFgpTWLpJyH6vVqcYYpUB8H8Dn` is Ready at `https://oases-chat-e1rvhx41b-qingtengs-projects.vercel.app`
ocli stable pending approval contract passed for Phase 123 Ocli session detail now exposes `pendingApprovals` independent of the recent event window, live approvals are actionable and persisted-only approval blockers remain visible but disabled, Web prefers this stable contract, and package/runtime version is prepared as 0.1.26; node --check, npx tsc --noEmit, root smoke, package smoke, standalone package test, unit tests, build, focused desktop Playwright approval/session-detail checks, and Vercel READY inspection passed; preview deployment `dpl_5r6fXAN1QcDy1bRjv1XLi68Ek6T1` is Ready at `https://oases-chat-opaosc4y9-qingtengs-projects.vercel.app`
ocli waiting approval status surface passed for Phase 124 Ocli health/session summaries now expose pendingApprovalCount and waitingForApproval, Web status strip/session rows render approval pending and waiting approval badges, root smoke/package smoke/unit/build/focused approval Playwright checks passed, and Vercel preview deployment `dpl_3PU9RYRST4sDg3JhdydiHunzE4ws` is Ready at `https://oases-chat-cbrioaqng-qingtengs-projects.vercel.app`
ocli continuation-needed status surface passed for Phase 125 Ocli health/session summaries now expose stoppedReason, needsContinuation, and continuationPendingCount for max-turn/open-todo sessions; Web status strip/session rows render continuation pending, latest needs continuation, needs continuation badges, and stopped reasons; package/runtime version prepared as 0.1.27; node --check, tsc, root smoke, package smoke, standalone package test, unit tests, build, focused desktop Playwright checks, Vercel READY inspection, and protected root HTTP 200 fetch passed; preview deployment `dpl_6YpM5EXNmGFeUS6beYB8tqr17rbw` is Ready at `https://oases-chat-84304h92s-qingtengs-projects.vercel.app`
ocli Web quick resume passed for Phase 126 needs-continuation session rows now expose a direct `继续运行` action that resumes without opening the detail panel first, uses a dedicated row/detail resume busy state, inserts the resumed session, and opens resumed detail; tsc, build, focused desktop Playwright session-detail/quick-resume/approval checks, and Vercel preview deployment `dpl_HxnuryRSQ5vj1xLZNeCZtcN2ae5N` passed; preview URL `https://oases-chat-h49g2exwr-qingtengs-projects.vercel.app` is READY and protected by Vercel Authentication
ocli approval-blocked resume contract passed for Phase 127 resume endpoint now rejects sessions with pending approval blockers before forking continuation work, returns HTTP 409 with an actionable approval message, and Web Resume readiness marks such sessions as approval pending with disabled `继续运行`; package/runtime version prepared as 0.1.28; node --check, tsc, root smoke, package smoke, unit tests, build, focused desktop Playwright session-detail/quick-resume/approval checks, Vercel READY inspection, and preview root HTTP 200 passed; preview deployment `dpl_6pPHmNgeHWUa4ruG1PrRDgZEDkHv` is Ready at `https://oases-chat-m7idabf50-qingtengs-projects.vercel.app`
ocli failed-session recovery continuity passed for Phase 128 failed Ocli sessions now surface as `needsContinuation` with `stoppedReason: failed`, resume prompts/context preserve source errors, failed-session resume requests include a compact original-request context, normal structured resume remains compact, Web Resume readiness shows failed stopped reason, and session rows can quick-resume failed sessions; node --check, tsc, root smoke, package smoke, unit tests, build, focused desktop Playwright failed-session/quick-resume/approval checks, Vercel READY inspection, and preview root HTTP 200 passed; preview deployment `dpl_7uYEX2sjdaXGQ2hhTPejY6s295tw` is Ready at `https://oases-chat-oxb5qsdem-qingtengs-projects.vercel.app`
ocli transient model request retry telemetry passed for Phase 129 retryable model/API failures now retry 408/409/429/5xx and network-style fetch errors up to the bounded `maxModelRequestRetries` default, skip non-retryable 4xx/abort paths, emit `model_request_retry` and `model_request_recovered` SSE telemetry, record `modelRequestRetryCount`, and render retry/recovered status in the Web project timeline; package/runtime version prepared as 0.1.30; node --check, tsc, root smoke, package smoke, unit tests, build, focused desktop Playwright model-retry/failed-session/approval checks, and Vercel READY inspection passed; preview deployment `dpl_FELU8MbnazV2egPTSnpWMhQzNgmc` is Ready at `https://oases-chat-chiythiwy-qingtengs-projects.vercel.app`
ocli model request parameter self-repair passed for Phase 130 Ocli now performs bounded one-session request-shape repair for explicit 400/422 parameter compatibility failures, including `invalid temperature: only 1 is allowed` -> `temperature=1` and unsupported `effort`/`reasoning_effort` -> field removal, without counting repairs as transient retries; Ocli emits `model_request_repair`, recovery telemetry carries `repairCount`, final results record `modelRequestRepairCount` and `modelRequestRepairs`, and Web project timelines render repair/recovered status; package/runtime version prepared as 0.1.31; node --check, tsc, root smoke, package smoke, unit tests, build, focused desktop Playwright model-retry/repair/failed-session/approval checks, package dry-run, and Vercel READY inspection passed; preview deployment `dpl_GD8xu5p1YJFycz9MM7DJDXhTQd1Q` is Ready at `https://oases-chat-97dlkpjfx-qingtengs-projects.vercel.app`
